---
id: RR-242
title: Coalesce Workspace route and activity project discovery
status: done
priority: high
depends_on: []
run_id: 27
canceled: false
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: "Lifecycle, cache invalidation, subprocess isolation, and three independent refresh consumers create a broad concurrency risk."
worker_provider_notes: "Preserve identical project routing and activity visibility for Codex and Claude; provider metadata may differ but refresh semantics must not."
---

## Description

Stop one-second notch, sleep, and dashboard refreshers from independently
reclassifying repositories, synchronously launching Git, rewriting unchanged
registry activation, and rescanning the same tickets. Build a cached
activity/run snapshot with explicit invalidation and move all subprocess and
filesystem discovery off the main actor.

RR-237 found repeated project and ticket discovery contributing hundreds of
milliseconds of sampled CPU while active sessions and the Workspace were
visible. This work should consolidate the consumers without changing routing
or activity visibility.

## Likely files or subsystems

- `Sources/relay-runner/App/AppState.swift`
- `Sources/relay-runner/Board/ProjectResolver.swift`
- `Sources/relay-runner/Board/ProjectRegistry.swift`
- `Sources/relay-runner/Board/ProgramBoardOverlayController.swift`
- `Sources/relay-runner/Process/ProcessManager.swift`
- `tests/RelayRunnerTests/ProjectRegistryTests.swift`

## Acceptance criteria

- [x] No `Process.waitUntilExit`, Git subprocess, registry write, or
      ticket-directory scan runs on the main actor during a steady active
      session.
- [x] Unchanged bridge/project state does not rewrite the registry or repeat
      repository classification.
- [x] Concurrent notch, sleep, and dashboard consumers share or coalesce one
      in-flight refresh and cancel it on service or session stop.
- [x] Route/scope preparation is below 5 ms p95 on main and the background
      activity refresh is below 25 ms p95 at 227 tickets.
- [x] Project Workspace, Program Workspace, workspace-root discovery, Codex,
      and Claude lifecycle regressions pass.

## Run log

**Run 27** (attempt 5) — branch `relay/rr-242`

- Added one cancellable background snapshot for Workspace route, run, ticket, notch, sleep, and dashboard scope consumers, with route and parsed-ticket cache reuse.
- Moved configured discovery, hosted activation, and steady bridge watchdog process checks off-main; unchanged Codex/Claude bridge metadata no longer rewrites the shared registry.
- Verified all 572 tests pass (3 opt-in benchmarks skipped normally); the RR-242 227-ticket benchmark measured 10.83 ms p95 against the 25 ms target, and cached route preparation passed the 5 ms p95 guard.

Unresolved questions or follow-ups: none. Ticket is done.
