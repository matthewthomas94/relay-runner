---
id: RR-290
title: Complete deferred signed installed artifact rollout verification
status: backlog
priority: high
depends_on: []
run_id: null
canceled: false
order: 290
execution_mode: implementation
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: The deferred gate requires a signed release identity, two physical Macs, mounted Workspace and permissions scenarios, provider parity, and externally reviewed evidence.
worker_provider_notes: Codex and Claude must pass equivalent project-scope, artifact, lifecycle, conflict, and recovery outcomes; launch, authentication, model, and effort differences remain separately documented.
---

## Description

Complete the signed installed two-device verification explicitly deferred from RR-289 on 2026-08-05. Use one exact Developer ID Application-signed Relay Runner bundle on two distinct physical Macs and the bounded RR-289 evidence manifest. This ticket preserves the missing work without treating source tests, disposable clones, an ad-hoc app, or the user's deferral as passing installed evidence.

Keep this ticket in backlog until the user is ready to perform the physical matrix. Passing the gate records evidence but does not automatically promote `new_project_default` or `legacy_migration_offer`; promotion remains a separate reviewed operator decision.

## Acceptance criteria

- [ ] One exact valid Developer ID Application-signed bundle is installed in `/Applications` on two distinct physical Macs, with matching version, build, bundle identifier, bundle hash, and signer Team ID recorded in the bounded template.
- [ ] Across the two installed records, empty Workspace, Add/Create/Remove, filesystem grant/revocation, moved/offline recovery, explicit scope, archive/history/restore, conflict UI, fresh install/reset, and mounted Workspace interactions pass.
- [ ] Installed dirty, staged, unstaged, untracked, and local-ahead source fixtures remain unchanged across artifact authoring, sync, lifecycle, archive/restore, migration, and recovery flows.
- [ ] Authenticated Codex and Claude sessions receive equivalent confirmed scope, snapshots, lifecycle events, conflicts, completion, and recovery outcomes; intentional executable/auth/model/effort differences are recorded separately.
- [ ] `relay-artifact-verify evaluate-installed` passes the one bounded manifest, an external reviewer accepts the immutable signed evidence, and release documentation records the exact artifact identity and any rejected deviation.
- [ ] Any decision to promote `new_project_default` or offer legacy migration is made separately after evidence review; neither cohort is enabled merely by completing this ticket.

## Rollback boundary

Verification must not mutate registered repository source state or enable a rollout cohort. On any mismatch or failed scenario, retain the evidence as rejected, keep later cohorts disabled, and repair or replace the signed build before retrying.

## Verification plan

Run `relay-artifact-verify inspect-installed` on the exact installed bundle, complete the bounded template on both devices, evaluate the combined manifest, review its privacy-safe hashes and provider coverage, then record immutable rollout evidence. Recheck rollout diagnostics before and after to prove no automatic promotion occurred.
