---
id: RR-76
title: Rename tray menu item to Show Status
status: backlog
priority: medium
depends_on: []
run_id: null
canceled: false
---

## Description

In the Relay Runner menu bar tray menu, the status action currently reads
`Show Program Status`. The menu item should read `Show Status` instead.

Scope this to the tray-menu label. The underlying action can continue to call
`showProgramStatus()`, and the status overlay title/body copy should stay as-is
unless a worker finds the same user-facing tray-menu string reused there.

Likely place to inspect:

- `Sources/relay-runner/MenuBar/MenuBarView.swift`

## Relay command

- sequence: 1
- id: 75870-1781253705331460000-1
- Newer Relay commands supersede stale ticket edits and dispatches.

## Acceptance criteria

- [ ] The Relay Runner menu bar tray action is labeled `Show Status`, not `Show Program Status`.
- [ ] Selecting the renamed menu item still opens the same program-status overlay behavior.
- [ ] No unrelated `Program Status` overlay titles, settings labels, test expectations, or model names are renamed unless directly required by the menu label change.
- [ ] Add/update a focused assertion if an existing menu-label test exists; otherwise verify with a targeted source search and a manual tray-menu check.
