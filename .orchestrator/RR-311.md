---
id: RR-311
title: Build a profile-based fresh-install test harness and clean-user test plan
status: done
priority: high
execution_mode: implementation
depends_on: []
run_id: 21
canceled: false
worker_model: balanced
worker_effort: high
worker_sizing_rationale: "The harness spans installer tooling, macOS state boundaries, recovery safety, and physical-machine validation."
worker_provider_notes: "Cover the installed app and both Codex and Claude integration state without deleting either provider's credentials or global configuration."
---

## Description

Extend the existing recoverable fresh-install tooling into explicit reset profiles and a deterministic evidence-capture workflow. The profiles must distinguish safe Relay-owned reset, separately confirmed voice-model/provider-integration reset, and a clean-user or VM validation level. Do not claim that a local reset recreates a brand-new macOS user when TCC, Keychain, notification authorization, Gatekeeper, or shared dependency caches remain.

Provide a practical second-Mac procedure based on disposable standard macOS users: retain an untouched administrator account, create a fresh test user, exercise installation/onboarding/first Workspace setup, collect the safe evidence bundle, then remove the test user with its home folder. VM snapshots are an optional faster reset route.

## Acceptance criteria

- [x] The harness has a read-only preview and a separate explicit execution confirmation for every destructive profile.
- [x] Relay-owned Application Support, preferences, caches, launch agents, and exact Relay temporary artifacts are inventoried, validated, and recoverably reset only after verified quiescence.
- [x] Voice models and named provider integrations require separate opt-in, are backed up or recoverable where appropriate, and never delete broad provider directories, global caches, credentials, repositories, or developer build state.
- [x] The harness reports the state it cannot reset, including TCC, Keychain, notifications, Gatekeeper, and shared machine state, and directs those checks to a clean user or VM.
- [x] Reinstall and first-run evidence capture records installer context, runtime version, startup outcome, and any generated incident/support bundle without capturing private source content.
- [x] Automated tests cover preview/execute gating, interrupted recovery, refusal around active processes, profile boundaries, and protection of non-Relay-owned paths.
- [x] Documentation provides a repeatable second-Mac clean-user procedure and a VM-snapshot alternative, including a checklist for both Codex and Claude setup states.

## Run log

### Run 19 (attempt 11) — branch `relay/rr-311`

- Recovered the reviewed profile-reset harness, manifest allowlists, symlink rejection, durable pre-move journals, and Codex/Claude provider-integration boundaries.
- Excluded only the fresh-install CLI's own process lineage from the default fail-closed Relay process scan; added a subprocess CLI regression that exercises that detector.
- Verified `python3 -m unittest discover -s tests -p 'test_fresh_install*.py'` (14 passing) and `git diff --check main...HEAD`.
- Done.

### Run 20 (attempt 12) — branch `relay/rr-311`

- Restored the complete run 19 implementation history, including all profile-reset protection and documentation commits.
- Restored a moved dangling `~/.claude/commands/relay-bridge.md` symlink after an injected interruption, while retaining target-symlink overwrite protection.
- Verified the regression and focused suite: `python3 tests/test_fresh_install.py` (15 passing), plus `py_compile` and `git diff --check`.
- Done.

### Run 21 (attempt 13) — branch `relay/rr-311`

- Recovered the complete run 20 code history, including durable journals, manifest allowlists, and fail-closed process inspection.
- Restored the exact allowlisted dangling Claude command symlink without dereferencing its backup leaf; retained manifest-parent and path allowlist protections.
- Verified `python3 -m unittest tests/test_fresh_install.py` (17 passing) and `git diff --check`.
- Done.
