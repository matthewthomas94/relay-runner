---
id: RR-146
title: Delegate worker review and merge to a follow-up agent
status: done
priority: high
depends_on: []
run_id: 219
canceled: false
worker_model: strong
worker_effort: high
worker_sizing_rationale: "Crosses orchestration policy, worker lifecycle, branch review, and merge behavior, with a high coordination blast radius."
worker_provider_notes: "Codex and Claude should expose the same orchestrator-to-worker review/merge handoff; document any provider-specific launch or permission differences."
---

## Description

Change the review and merge workflow so the foreground orchestrator does not directly review and merge an implementation worker's branch after that worker finishes a ticket. Instead, when an implementation worker completes, the foreground orchestrator should hand off the completed worker branch to a new review/merge sub-agent.

The review/merge sub-agent should inspect the completed worker's changes, run the appropriate verification, decide whether the branch is acceptable, and merge it into the working branch when it passes. The foreground orchestrator remains responsible for reporting status to the user and preserving the same project/command metadata guarantees, but the substantive review and merge work moves into the follow-up worker.

The implementation should preserve provider parity: Codex and Claude sessions should follow the same review/merge handoff model, with only documented provider-specific command, permission, or model differences.

## Acceptance criteria

- [x] The documented orchestration workflow says completed implementation workers are reviewed and merged by a dedicated follow-up review/merge worker, not directly by the foreground orchestrator.
- [x] The runtime workflow can create or dispatch a review/merge worker after an implementation worker finishes, carrying enough context to identify the original ticket, worker branch, target branch, and verification expectations.
- [x] The review/merge worker can report success, failure, and required follow-up in a way the foreground orchestrator can summarize to the user.
- [x] The merge path preserves the ticket lifecycle and board behavior, including publishing `done` only after the worker branch has been accepted and merged.
- [x] Provider-facing behavior is considered for both Codex and Claude, with intentional differences documented in code, workflow docs, or user-facing status text.
- [x] Tests or documented manual verification cover successful review/merge, review failure, merge conflict, and provider-specific launch handling.

## Run log

- **Run 219** (attempt 1) — branch `relay/rr-146`
  - Added a provider-neutral review/merge worker handoff: implementation completion now dispatches a reviewer with ticket, run, branch, target, and verification context; foreground `review_run` now enqueues that reviewer, while a private decision endpoint performs accept/retry.
  - Updated board/program status, Graphify normalization, MCP tool descriptions, and orchestration docs so `Reviewing` is active work and `done` is only published after accepted merge.
  - Added regression coverage for automatic/manual review dispatch, review-worker failure reporting, successful merge, merge conflict handling, and Codex/Claude launch flags.
  - Verification: `python3 -m unittest tests.test_orchestrator_dispatch tests.test_pm_frontstage tests.test_program_status tests.test_graphify_ingest`; `swift test`.
  - Status: done. No unresolved follow-ups.
