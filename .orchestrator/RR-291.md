---
id: RR-291
title: Restore the installed Workspace daemon after RR-277 packaging
status: done
priority: high
execution_mode: implementation
depends_on: []
run_id: null
canceled: false
order: 291
worker_model: strong
worker_effort: high
worker_sizing_rationale: The repair is small, but it crosses installed-bundle composition, launchd recovery, onboarding rollout configuration, and mounted Workspace verification.
worker_provider_notes: The orchestrator service and project-free onboarding gate are provider-neutral; Codex and Claude must reach the same healthy Workspace surface.
---

## Description

Repair the current locally rebuilt Relay Runner bundle, whose launchd orchestrator repeatedly exits because `orchestrator.py` imports RR-277's `followup_tickets.py` but `scripts/build-dmg.sh` does not copy that required module into the app. The missing daemon leaves Workspace's **Checking for updates** reveal blocking the user's onboarding test.

Package the required service unconditionally and extend the existing bundle-content regression test. Rebuild and validate the staged and installed bundles, restart the daemon, and relaunch this local test instance with registry-v2 enabled so project-free onboarding can be exercised immediately. Do not change the production registry-v2 default or advance any evidence-gated rollout cohort.

## Acceptance criteria

- [x] The canonical DMG build copies `followup_tickets.py` into the app's SharedSupport services directory and fails rather than silently omitting required services.
- [x] Automated packaging coverage asserts that RR-277's service remains in future bundles.
- [x] The staged and installed bundle contain the module, pass strict deep code-sign verification, and use matching app and service hashes.
- [x] The installed launchd orchestrator reaches its health endpoint without a new missing-module traceback.
- [x] The local test app is relaunched with registry-v2 enabled while the source default and staged rollout cohorts remain unchanged.
- [x] Mounted onboarding can finish without choosing a workspace, and Workspace leaves **Checking for updates** for an empty or registered-project state.

## Verification plan

Run the focused packaging test, build with `scripts/build-dmg.sh`, inspect and hash the staged bundle, install it, restart the exact Relay Runner app and orchestrator launch agent, and query daemon health. The user performs the final mounted onboarding and Workspace smoke.

## Inline implementation log

- Added `followup_tickets.py` to the canonical unconditional service bundle list and extended the existing bundle-content regression test. The focused unittest and `git diff --check` passed.
- `scripts/build-dmg.sh` completed and installed the ad-hoc local test bundle. Strict deep code-sign verification passed for staged and installed apps; staged and installed SHA-256 values matched for the app executable, `orchestrator.py`, and `followup_tickets.py`. The installed runtime imported both daemon modules successfully.
- The launchd daemon recovered and `GET /v1/health` returned `ok: true`; the existing missing-module traceback count remained unchanged after recovery.
- Relaunched installed PID 45352 after setting the user-session `RELAY_RUNNER_REGISTRY_V2=1` test opt-in. The production source default and artifact rollout cohorts were not changed. Moved the prior `.onboarded` sentinel recoverably to `/private/tmp/relay-runner-onboarding-backup.fyzMWC/.onboarded` so this launch enters fresh onboarding; mounted UI confirmation remains with the user.
- Remote v0.4.41 verification reached Workspace, but **Checking for updates** ended in the local workspace service reconnect error because the daemon never emitted its first launch event. This does not invalidate the verified `followup_tickets.py` package repair; the remaining visible check is blocked on the distinct RR-309 pre-main startup regression.
- On 2026-08-12, the user completed the remaining mounted check with the installed signed v0.4.43 build and confirmed onboarding reached a usable Workspace without the prior loading loop. This is user-observed UAT; RR-309 retains the implementation and release evidence for the startup repair.
