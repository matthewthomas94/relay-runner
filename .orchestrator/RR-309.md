---
id: RR-309
title: Make first-run Workspace startup deterministic and diagnosable
status: verification_blocked
priority: urgent
execution_mode: implementation
depends_on: []
run_id: 26
verification_blocker: A signed build containing run 26 is not available in this isolated worktree for the required clean-user or second-Mac first-Workspace check with both Codex and Claude.
verification_resume: Build and sign Relay Runner with commit 524269e, then verify first Workspace creation on a clean macOS user or second Mac with both Codex and Claude provider setups.
canceled: false
order: 309
worker_model: strong
worker_effort: high
worker_sizing_rationale: Cross-process startup, macOS launchd, onboarding, and registry changes require careful integration and clean-machine verification.
worker_provider_notes: Verify equivalent startup behavior for Codex and Claude provider setup; provider launch commands may differ.
---

## Description

Resolve the verified and high-confidence first-run Workspace reliability gaps identified in the startup audit. The service runtime must not accept a Python version that later fails to import the orchestrator. Onboarding must establish real orchestrator readiness rather than deferring installation to the first Work request, and readiness must reflect the loaded launchd job and health endpoint rather than only a plist on disk.

Also make project creation and Workspace reopening reliable on cold state: avoid blocking Git work on the UI path, await registry/route readiness before opening Work, and make Registry V2 the consistent source for dashboard and ready-ticket sweep behavior. Preserve existing provider parity, user data, and non-destructive recovery behavior.

## Acceptance criteria

- [x] The supported service runtime either requires Python 3.11 or later, or provides and tests a compatible TOML import path for every accepted Python version.
- [x] Onboarding installs or verifies the orchestrator and reports a bounded, actionable failure with retained safe diagnostics when it cannot become healthy.
- [x] Readiness verifies launchd/process state and the orchestrator health endpoint; a stale, unloaded, or crashing launch agent triggers repair or a clear recovery path.
- [x] Project creation does not perform unbounded Git work on the main UI path, and Workspace opens only after the newly registered project route is available.
- [x] Dashboard ingestion and ready-ticket dispatch use the same Registry V2 project model, with regression coverage for a newly registered V2 project.
- [x] Automated coverage includes Python 3.10-or-supported-version behavior, stale launch-agent recovery, daemon crash/slow readiness, cold project creation, and Registry V2 dispatch.
- [x] Every retained venv is version-checked before the dependency-ready short circuit; an unsupported interpreter is replaced safely even when its existing dependencies still import.
- [x] The selected venv imports the complete shipped orchestrator before setup reports ready, including the artifact lifecycle modules loaded before `main()`.
- [x] A pre-`main()` daemon failure records a bounded safe summary with the selected Python version, stops rapid launchd restart churn, and reaches an actionable Workspace failure within a bounded interval.
- [x] Automated coverage reproduces an unsupported retained venv with otherwise healthy dependencies and verifies upgrade, import preflight, health, bounded retry behavior, and safe diagnostics.
- [ ] A clean-user or second-Mac run verifies first Workspace creation for both Codex and Claude setups, with any unavailable external checks recorded explicitly.

## Run log

### Run 5 (attempt 1) — branch `relay/rr-309`

- Added Python 3.10 TOML compatibility, made onboarding install and health-check the orchestrator, and hardened launchd recovery with bounded retained diagnostics.
- Moved project Git registration off the main thread, delayed Workspace reopening until route refresh completes, and unified dashboard/status/ready-sweep reads on Registry V2.
- Added startup, stale/crashing launch-agent, cold project registration, and newly registered Registry V2 dispatch coverage; 54 targeted Swift tests and 23 targeted Python tests pass.

Implementation is complete and provider-neutral for Codex and Claude. Verification is blocked only on the clean-user or second-Mac first-launch run named above; the full Python discovery run was stopped after its artifact integration cases proved unusually long, while targeted suites and changed-module import checks passed.

### Remote v0.4.41 verification failure

- A remote macOS 26.6.1 run of Relay Runner 0.4.41 build 45 failed the external Workspace check under incident `inc-fda6cd599a82`: the support timeline contains 650 shell setup starts and 649 setup-ready outcomes across roughly six hours, but no `orchestrator_launch` event.
- Nine Workspace readiness attempts were recorded. Attempts 9, 10, 12, and 13 each ended after approximately 20–24 seconds with the local workspace service reconnect failure shown to the user.
- The shipped service set is complete. The absence of the daemon's first event localizes the failure to interpreter startup or module import before `main()`; the support bundle does not include the selected interpreter version or pre-main stderr, so the exact remote exception is not proven.
- A retained unsupported venv is a concrete regression path: setup currently treats successful dependency imports as sufficient without checking that venv's version, while the shipped artifact modules fail to import on Python 3.9. This hypothesis must be confirmed or ruled out by the new preflight evidence rather than reported as the remote root cause prematurely.

The prior implementation remains useful, but its external verification failed. RR-309 is reopened in backlog pending the runtime/pre-main repair and a repeat remote check for both providers.
- **Verification resumed after run 5** — Remote testing on Relay Runner 0.4.41 build 45 changed the external condition: Workspace startup failed under incident inc-fda6cd599a82. The support bundle shows 650 setup starts, 649 setup-ready outcomes, zero orchestrator_launch events, and repeated local workspace service reconnect failures. Resume implementation to repair the pre-main runtime/import path, bounded retry behavior, and diagnostics, then repeat signed remote verification.

### Run 26 (attempt 3) — branch `relay/rr-309`

- Version-checks every retained venv before dependency imports can short-circuit setup and recreates unsupported Python 3.9/3.14 environments with a supported 3.10–3.13 interpreter.
- Preflights the complete shipped `services.orchestrator` import before outer setup readiness; failures retain only the bounded exception class and selected Python version, surface recovery text, and exit successfully under launchd to stop KeepAlive churn.
- Added provider-neutral regressions for retained-venv replacement and pre-`main()` failure diagnostics; 8 startup tests, 3 shell-diagnostics tests, both shell syntax checks, and a real Python 3.13 orchestrator import pass.

Implementation is complete for Codex and Claude. Verification is blocked only on repeating first Workspace creation from a signed build on a clean macOS user or second Mac with both provider setups.
