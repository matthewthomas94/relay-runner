---
id: RR-79
title: Compact tray menu service status
status: done
priority: medium
depends_on: []
run_id: 70
canceled: false
---

## Description

The Relay Runner tray/menu bar menu can become too wide when `serviceLifecycleMessage` contains a long error such as `Bundled service refresh failed: relay-orchestrator --restart-if-idle failed with exit code 1.` The screenshot from June 12, 2026 shows the entire menu stretched across much of the screen.

Keep the menu compact while still surfacing the actionable state. The long service lifecycle message should not determine the menu width. Prefer a short visible label plus a way to inspect the full detail, such as a tooltip, secondary details window, or truncated text that remains readable.

## Acceptance criteria

- [x] The tray menu remains compact when `serviceLifecycleMessage` contains a long string.
- [x] The user can still discover the full service lifecycle error text without needing logs.
- [x] Menu labels such as Start Session, Record, Replay, Show Board, Show Status, Settings, Check for Updates, and Quit remain readable.
- [x] Add or update a focused Swift test for the formatting/truncation helper or menu-state model if the UI text is factored outside the view.
- [x] Verify with `swift test --skip-update`.

## Run log

- **Run 70** (attempt 1) - branch `relay/rr-79`
  - Added a compact service lifecycle status formatter for menu labels while preserving the full detail text.
  - Updated the tray menu lifecycle item to show the short label, expose the full text via tooltip, and open it in the Service Status overlay.
  - Added focused Swift tests and verified with `swift test --skip-update` (135 tests passed). Provider parity: no provider-facing behavior changed.
  - Unresolved questions or follow-ups: none. Ticket done.
