---
id: RR-302
title: Remove abandoned demo-only loader and packaging changes
status: in_progress
priority: high
execution_mode: implementation
depends_on: []
run_id: null
canceled: false
order: 302
worker_model: fast
worker_effort: medium
worker_sizing_rationale: The cleanup is limited to four known files, but it must preserve the released loader and packaging behavior exactly while protecting unrelated working-copy changes.
worker_provider_notes: none
---

## Description

Remove the abandoned demo-only Working-loader V2 and app-only packaging changes that are keeping the canonical checkout dirty and blocking integration of the already reviewed RR-299 worker run.

Preserve the released product baseline: Working uses the standard shared 0.6-second native motion rather than the demo-only 2-second `-18.85`-radian rotation; Listening and Playing retain their existing motion; Reduce Motion remains respected; and `scripts/build-dmg.sh` retains its normal release and `--debug` packaging behavior without the demo-only `--app-only` mode. The obsolete RR-296 board card is PM-owned cleanup and must not be recreated or edited by the worker. Do not inspect, modify, stage, or publish `.demo-private` content.

Limit source cleanup to these paths:

- `Sources/relay-runner/Board/BoardRevealTransition.swift`
- `Sources/relay-runner/Overlay/NotchStatusController.swift`
- `tests/RelayRunnerTests/NotchStatusPlacementTests.swift`
- `scripts/build-dmg.sh`

After cleanup, resume the preserved RR-299 integration path rather than dispatching a new RR-299 implementation attempt.

## Acceptance criteria

- [x] Working retains the released standard 0.6-second native motion; no demo-only 2-second duration or `-18.85`-radian rotation remains.
- [x] Listening, Playing, Not working, board-reveal rendering, and Reduce Motion behavior remain unchanged from the released baseline.
- [x] `scripts/build-dmg.sh` has no demo-only `--app-only` argument or early-exit path, while the default and `--debug` packaging interfaces remain intact.
- [x] RR-296 remains absent and is not recreated or edited by the worker; the worker changes only RR-302 under `.orchestrator/`.
- [x] No `.demo-private` file is inspected, modified, staged, published, or included in artifacts.
- [x] `git diff --check`, `bash -n scripts/build-dmg.sh`, and `swift test --filter NotchStatusPlacementTests` pass.
- [ ] The four named source/build paths no longer keep the canonical checkout dirty, and the preserved RR-299 reviewed run can resume integration without a new implementation attempt.

## Run log

- 2026-08-09: Executed inline at the user's request. Discarded only the four named demo-only working-copy diffs and preserved the approved RR-296 deletion. Confirmed the V2 duration/rotation and `--app-only` markers are absent. `git diff --check`, `bash -n scripts/build-dmg.sh`, and all 30 `NotchStatusPlacementTests` passed. RR-299 run 21 still needs its existing reviewed integration resumed after this cleanup commit makes the canonical checkout clean.
