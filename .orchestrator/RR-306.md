---
id: RR-306
title: Prepare and publish Relay Runner v0.4.36
status: in_progress
priority: high
execution_mode: implementation
depends_on: []
run_id: 23
canceled: false
order: 306
worker_model: codex:sol
worker_effort: xhigh
worker_sizing_rationale: Inherited provider, model, and effort from Relay Runner General Settings.
worker_provider_notes: Use my defaults preserves explicit stable provider selections; Codex resolves Sol/Terra/Luna then uses model_reasoning_effort and Claude uses --effort.
---

## Description

Prepare Relay Runner v0.4.36 (build 40) from the current canonical `main` branch. The local branch contains the reviewed Relay Runner changes since v0.4.35 and is 32 commits ahead of the common release base; `origin/main` also contains the intentional README-only commit `aadffb4` that moves the existing installation and build instructions. Reconcile both histories without rewriting published history, dropping local work, or force-pushing.

The worker owns only release preparation: integrate `origin/main`, bump the root `Info.plist` from v0.4.35/build 39 to v0.4.36/build 40, run the release preflight, and commit the prepared branch. Do not push any branch, create or push a tag, create a GitHub release, modify the update repository, install the app, or relaunch Relay Runner. Leave the ticket verification-blocked on the foreground-owned publication gate after all locally verifiable criteria pass.

The foreground PM owns review integration and the external release sequence: push canonical `main`, create and push annotated tag `v0.4.36`, wait for signed/notarized CI publication, verify both GitHub release repositories and the Sparkle appcast from fresh downloads, then install and validate the released bundle without interrupting the active Relay session.

Preserve ignored private demo material locally and outside Git and release artifacts. Do not inspect, modify, stage, log, publish, or package `.demo-private` content.

## Acceptance criteria

- [ ] Canonical history preserves both the local post-v0.4.35 commits and remote README commit `aadffb4`; no published history is rewritten and no force push is used.
- [ ] Root `Info.plist` declares `CFBundleShortVersionString` `0.4.36` and monotonically increasing `CFBundleVersion` `40`, with no unrelated version or source changes.
- [ ] The prepared release includes the completed RR-279, RR-297, RR-298, and RR-299 product changes while retaining the released standard Working motion and normal packaging interface restored by RR-302.
- [ ] `.demo-private` content is not inspected, modified, staged, logged, published, or included in release artifacts.
- [ ] `git diff --check`, the full Swift test suite, the full Python service test suite, and `RELAY_SKIP_APPLICATIONS_REFRESH=1 ./scripts/build-dmg.sh` pass on the reconciled release-preparation branch.
- [ ] The worker commits only the reconciled history, version bump, and its own RR-306 evidence, then records `verification_blocked` for the foreground-owned public release steps; it does not push, tag, publish, install, or relaunch.
- [ ] The foreground PM merges the reviewed preparation, pushes canonical `main`, creates and pushes annotated tag `v0.4.36`, and confirms the tag targets the reviewed release commit.
- [ ] The tagged GitHub Actions release workflow succeeds with signing, notarization, stapling, Sparkle signing, and publication of `RelayRunner.dmg`, `RelayRunner.zip`, and `appcast.xml` to both `relay-runner` and `relay-runner-updates`.
- [ ] Freshly downloaded assets from both repositories match by SHA-256; the appcast reports v0.4.36/build 40 with a valid Sparkle signature and points at the v0.4.36 update asset.
- [ ] Fresh public artifacts pass deep code-signature verification, Gatekeeper assessment, and stapler validation; the installed `/Applications/Relay Runner.app` reports v0.4.36/build 40, matches the verified release executable, contains required services, and does not interrupt the active Relay session.

## Run log

- 2026-08-09: PM preflight confirmed a clean local `main`, latest public tag v0.4.35/build 39, 32 local-only commits, and one remote-only README commit `aadffb4`. Both v0.4.35 release repositories and the prior signed workflow are healthy. Prepared v0.4.36/build 40 as the next monotonic release.
- 2026-08-09: Run 22 reconciled the README history, prepared v0.4.36/build 40, passed 703 Swift tests with 3 skips, and passed local packaging, but the Python suite failed 8 of 579 tests in `tests.test_ready_sweeper`. Focused reproduction on canonical `main` confirmed RR-299's real Git cleanliness check correctly rejects the suite's fake `.git` directory fixtures. RR-306 is deferred behind RR-307; no push, tag, publication, install, or relaunch occurred.
- 2026-08-09: Run 23 started before the RR-307 dependency was recorded and absorbed the same minimal fixture-only repair by initializing a real temporary Git repository in `ReadySweeperTests.make_repo`. All 18 focused ready-sweeper tests pass; full Swift and Python validation is running. RR-307 is superseded without dispatch.
- **Run 25** (attempt 4) — branch `relay/rr-306`
  - Aborted before claiming or implementation because RR-306 is already active under `run_id: 23`, which does not match this dispatch's run ID.
  - No source, version, release, publication, installation, or ticket-frontmatter changes were made; the ticket remains partial and `in_progress` pending orchestrator reconciliation.
