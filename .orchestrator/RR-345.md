---
id: RR-345
title: Distinguish information queries from project-work commands
status: done
priority: high
execution_mode: implementation
depends_on: []
run_id: 104
canceled: false
order: 345
worker_model: strong
worker_effort: high
worker_sizing_rationale: "The fix changes shared mutation routing and must preserve command, ticket, cancellation, and provider-parity behavior."
worker_provider_notes: "Keep classification and disposition semantics identical for Codex and Claude; both providers must pass the same end-to-end routing regressions."
---

## Description

Prevent read-only information queries from becoming project mutations merely because they mention words associated with work or refer to tickets. A question whose desired output is an answer or evidence should remain in the foreground PM and may use bounded read-only inspection without creating, editing, or dispatching a ticket.

The observed failure was a release-history question about RR-325 through RR-343. Both the original question and the clarification that it meant a release rather than a build were classified as `create_ticket`, because command classification treats a bare occurrence of `build` as work regardless of whether it is a noun, historical context, or part of an interrogative sentence. When a normalized ticket ID is present, an unmatched question can also fall through to `update_ticket`. Intent arbitration then correctly queues the incorrectly labeled action as a repository mutation.

Use this decision boundary:

- An **information query** requests an answer, explanation, status, history, comparison, or evidence and does not authorize a durable state change. Handle it inline with no new ticket.
- **Project work** requires positive intent to change durable state or create a durable deliverable. Work nouns, historical verbs, and ticket references are context, not mutation authority.
- A **mixed query and mutation** must be split into ordered intents or clarified. Answering the query must not silently authorize the conditional mutation.
- Complexity alone must not silently promote a query into a ticket. A sustained read-only investigation becomes a tracked spike only when the user explicitly asks to queue, delegate, or track it, or affirmatively accepts that proposed escalation.

Preserve the existing foreground handling for screen observation, direct computer actions, session operations, speech controls, and explicit orchestration corrections.

## Acceptance criteria

- [x] The exact disfluent release-history question and its “release, not build” clarification classify as non-mutating information queries, with `requires_ticket: false`, no repository mutation claim, and a `continue_current` disposition.
- [x] Interrogative and epistemic forms such as “Was there a build?”, “Which release included these tickets?”, “Did we ship this?”, “Why did the build fail?”, and “Can you tell me whether RR-325 is done?” never create or update tickets solely because they contain work words or ticket IDs.
- [x] Positive mutation requests such as “Build the release”, “Can you build a release?”, “Update RR-325”, and “Dispatch RR-325” retain their existing create, update, and dispatch behavior.
- [x] Ticket IDs are treated as references rather than mutation authority: read-only ticket questions use a non-mutating inspection/query path, while updates require explicit change intent.
- [x] Mixed requests such as “Is there a release, and if not, build one” are split into ordered query/work intents or produce one concise clarification; the query clause alone cannot authorize the work clause.
- [x] Query results do not automatically create follow-up tickets. Escalation to a tracked spike requires explicit user tracking/delegation intent or affirmative acceptance after the PM proposes it.
- [x] End-to-end voice metadata proves information queries produce no visible ticket file, no counter change, no dispatch, and no project mutation authorization for both Codex and Claude.
- [x] Existing direct-action, inline-session-operation, cancellation, interruption, pending-work, orchestration-correction, and ordinary project-work regressions continue to pass.

## Run log

- **Run 104** (attempt 11) — branch `relay/rr-345`
  - Restored the validated attempt-10 classifier, voice metadata, arbitration, and daemon regressions, then classified operative clauses after leading ticket context so both requested RR-325 mutations retain `update_ticket` authority.
  - Kept modal `we` status and hypothetical questions read-only while preserving direct `you`, imperative, `let's`, and explicit-need mutation forms for Codex and Claude.
  - Verified 278 expanded classifier, arbitration, voice metadata, daemon lifecycle, and dispatch tests; all passed. Ticket done with no unresolved follow-ups.
