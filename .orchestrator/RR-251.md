---
id: RR-251
title: Run the onboarding voice tutorial locally with immediate replay
status: done
priority: high
depends_on: []
run_id: 7
canceled: false
order: 251
worker_model: strong
worker_effort: high
worker_sizing_rationale: This onboarding latency and replay fix crosses session startup, STT delivery isolation, deterministic TTS state, gesture consumption, and provider-side-effect safety.
worker_provider_notes: Codex and Claude must bypass their provider-specific session launch paths and use identical single-gesture local replay behavior.
---

## Description

Make the session-controls onboarding practice exchange fully local and deterministic so it teaches recording, send, play, replay, and cancel without launching a Codex or Claude session or waiting for a messenger/orchestrator response. During the tutorial’s “say hi” recording step, accept any non-empty spoken content as completion input but do not publish it to the Relay voice command path. Immediately inject the fixed tutorial reply `Hello, how are you?` into the existing local TTS/message-waiting flow. The first Option double-tap should play that reply. When initial playback finishes and the tutorial advances to the replay/cancellation lesson, the next Option double-tap must both surface the replay/cancellation pill and immediately restart the same audio on that single gesture; it must not consume one gesture for UI state and require a second gesture for playback. Control double-tap should then stop playback and advance the tutorial. Keep microphone/STT and TTS behavior real so users practice the actual controls, while isolating all provider, bridge command, ticket, and external side effects. Apply the same behavior for Codex and Claude.

## Acceptance criteria

- [x] Entering the session-controls tutorial does not start or require a Codex or Claude provider session, messenger process, orchestrator session, or Relay voice-command consumer.
- [x] The tutorial can begin recording through the normal Caps Lock control using local microphone/STT behavior even when no provider session is running.
- [x] When the user sends any non-empty tutorial recording, its transcript is consumed only by the tutorial and is never written to the voice command inbox, FIFO, ready prompt, provider stdin, intent classifier, or ticket workflow.
- [x] After tutorial send, the local message-waiting state deterministically contains exactly `Hello, how are you?` without invoking a model or network-backed service.
- [x] The first Option double-tap plays the deterministic reply through the normal local TTS path.
- [x] After initial playback completes, one Option double-tap both presents the replay/cancellation pill state and immediately restarts the same deterministic reply; no second Option gesture is required.
- [x] The replay gesture is delivered exactly once: it does not double-start playback, leave a stale play request, or advance the tutorial twice.
- [x] Control double-tap during the cancellation lesson stops active playback through the normal control path and advances the tutorial.
- [x] Tutorial startup and response latency no longer depend on provider authentication, model warmup, bridge readiness, or messenger availability.
- [x] Outside the active onboarding tutorial, session startup, recording delivery, messenger/orchestrator behavior, ordinary TTS replay, and control gestures remain unchanged.
- [x] Codex and Claude follow the same local deterministic tutorial path.
- [x] Focused tests prove no provider/session launch and no command publication, assert the exact deterministic message, reproduce the first-tap-pill-without-playback regression, verify single-gesture replay, and cover send/play/replay/cancel plus tutorial resume behavior.

## Run log

### Run 7 (attempt 1) — branch `relay/rr-251`

- Replaced provider/bridge tutorial startup with an isolated local STT/TTS path that consumes recordings in-app and immediately stages exactly `Hello, how are you?`.
- Serialized tutorial reply, play, replay, and cancel events through the local TTS worker so one Option gesture restarts playback exactly once and Control stops it without publishing voice commands.
- Added provider-neutral Swift coverage and TTS isolation/race coverage; 95 focused Swift tests and all 364 Python tests pass.

Follow-up: the full Swift suite remains red in unrelated pre-existing launch-model catalog assertions in `ProcessManagerLaunchTests`; the RR-251-focused launch test and all affected suites pass. Ticket done.
