---
id: RR-177
title: Move onboarding into the notch and correct the native permission drag companion
status: backlog
priority: urgent
depends_on: []
run_id: null
canceled: false
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: "This release-blocking second pass spans first-run window lifecycle, notch-hosted interaction, AppKit drag pasteboard interoperability, companion geometry, motion, and end-to-end verification against System Settings."
worker_provider_notes: "Provider-neutral Relay Runner UI: Codex and Claude must receive the same notch onboarding and permission guidance while retaining their existing provider-specific authentication and model configuration behavior."
---

## Description

Replace the post-intro `Welcome to Relay Runner` titled window with a notch-owned onboarding flow and correct the RR-172 permission companion so it is both visually faithful and genuinely draggable into macOS System Settings.

Use the supplied references as the visual authority:

- Current unwanted window: `/Users/matthewthomas/Desktop/Screenshot 2026-07-19 at 8.12.40 pm.png`
- Target Input Monitoring composition: `/Users/matthewthomas/Downloads/Session active - Input monitoring.jpg`

The existing first-launch Workspace pull-down/typewriter intro remains. When it retracts, onboarding must not construct or show the current 560×680 titled `NSWindow`. The old `Welcome to Relay Runner` window must also stay absent from resumed, simplified, recovery, and manual re-run setup paths. Onboarding is owned by the top-center notch surface: retain the existing compact status labels and host any decision that genuinely requires controls in a borderless, notch-attached surface that expands from and retracts into the notch. Preserve the existing provider/model selection, workspace choice, Python bootstrap, provider authentication, resume, Skip, Ready, and Start Session outcomes rather than silently dropping them. During permission steps, collapse to the compact notch status and let Apple's dialog or System Settings plus the companion be the only other visible surfaces. In particular, microphone shows `Microphone` in the notch and Apple's ordinary consent dialog only—no Relay Runner setup window and no draggable companion.

Rework the companion against the target design rather than iterating on the current card. It is a compact approximately 140×142-point pure-black tile with approximately 16-point continuous corners, no generic card chrome, no visible border in the normal contrast mode, no Finder/gear button row, and no unrelated outer container. Center the real installed Relay Runner `R/` app icon at approximately 88–92 points with a short white `Drag me` label beneath it. Keep non-drag alternatives available through accessibility/context-menu behavior without changing the visual composition.

The tutorial cursor is not a hand-drawn arrow. Use the native macOS open-hand image/hotspot while approaching, the native closed-hand image/hotspot while pressed and dragging, and return to the open hand after release. The icon ghost and green drop cue may remain, but their geometry must begin at the visible icon and travel to the actual permission-list/`+` target without obscuring System Settings. The full-screen demo panel remains click-through and must never move the user's real pointer.

Fix the actual drag at the AppKit boundary. Use the application URL itself as the native `NSPasteboardWriting`/`NSDraggingItem` writer (with a standard file URL and Finder-compatible file-list representation where needed), copy semantics, the initiating `mouseDragged` event, and a Finder-like drag image anchored beneath the real cursor. Resolve and validate an installed `.app` URL before enabling the gesture. The payload must be accepted when dropped into Accessibility, Input Monitoring, and Screen Recording lists on supported macOS versions; do not rely on the current hand-built single-type `NSPasteboardItem` if System Settings rejects it. Preserve first-mouse handling, threshold/one-session guards, drag lifecycle deferral, companion following, Reduce Motion, VoiceOver, and teardown behavior from RR-172.

Do not modify or discard the unrelated in-progress Settings styling changes currently present in the source working tree. Work only in the isolated ticket worktree.

## Acceptance criteria

- [ ] Automatic fresh onboarding keeps the existing Workspace/typewriter intro, then continues through a notch-owned flow without ever constructing or displaying a titled `Welcome to Relay Runner` window.
- [ ] Simplified/resumed onboarding, missing-permission recovery, and `Re-run Setup Walkthrough…` also use the notch-owned flow; closing/skipping/completing it cleans up all notch panels, permission companions, observers, and timers.
- [ ] Provider/model choice, working-directory choice, Python installation, provider authentication, resume persistence, Ready, and Start Session retain their existing outcomes; required controls appear only in a borderless surface visually attached to and animated from the notch.
- [ ] During microphone setup, the compact notch reads exactly `Microphone` and only Apple's native microphone dialog appears; no Relay Runner onboarding window or companion tile is visible.
- [ ] During Accessibility, Input Monitoring, and Screen Recording setup, the compact notch shows the existing exact permission label while System Settings and one companion are the only other visible surfaces.
- [ ] The normal companion matches the supplied design: approximately 140×142 points, pure black, approximately 16-point corners, no ordinary border/shadow/card chrome, no folder/gear row, a centered 88–92-point installed `R/` icon, and centered white `Drag me` copy.
- [ ] The false cursor uses native macOS open-hand and closed-hand artwork with correct hotspots and phase changes; no custom arrow path is drawn, and the demo never posts input or changes the real cursor.
- [ ] A real gesture from the visible app icon starts one native AppKit file drag from the current `mouseDragged` event with copy semantics and Finder-compatible URL/file representations; dropping it into each supported System Settings privacy list adds Relay Runner when absent.
- [ ] The drag image is Finder-like and remains under the real pointer; real hover/down/drag pauses the demo and follower, and drag completion resumes/finishes exactly once without premature grant advancement.
- [ ] Invalid/non-installed bundle paths disable dragging and retain Reveal-in-Finder plus `+`-button instructions through context menu and accessibility without adding visible controls to the reference tile.
- [ ] Window discovery, cross-display placement, click-through demo behavior, permission polling, grant acknowledgement, Reduce Motion, Increase Contrast, VoiceOver, hosted-tool recovery, and teardown guarantees from RR-172 remain intact.
- [ ] Pure tests cover the no-titled-window onboarding route, notch-surface lifecycle and state preservation, companion style constants, native cursor phase selection, pasteboard representations/drag operation, and drag lifecycle; focused and full Swift test suites pass.
- [ ] A release app is built and the companion is manually smoke-tested against at least Input Monitoring in System Settings, with the exact verification result recorded in the ticket run log.
