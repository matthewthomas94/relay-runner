---
id: RR-199
title: Extend onboarding title matrix and cut v0.4.26
status: backlog
priority: high
depends_on: [RR-198]
run_id: null
canceled: false
---

## Description

Extend the fully typed `Relay Runner /` dot-matrix phase in the intro from 2 seconds to 4 seconds, update the timing regression coverage, and publish the next Relay Runner patch release from the integrated `main` branch.

Advance `CFBundleShortVersionString` from `0.4.25` to `0.4.26` and the monotonic Sparkle `CFBundleVersion` from `29` to `30`. Verify Swift and Python tests plus local packaging, push `main`, create and push annotated tag `v0.4.26`, then monitor the signed/notarized GitHub Actions release through source and public Sparkle asset publication.

Execute the small timing change and authenticated release preparation inline on `main`; do not dispatch a worker for the publication step.

## Acceptance criteria

- [ ] `OnboardingIntroTimeline.dotFieldTravel` is exactly 4 seconds and its timing tests cover the new duration.
- [ ] `Info.plist` reports `CFBundleShortVersionString` `0.4.26` and `CFBundleVersion` `30`.
- [ ] The full Swift and Python test suites pass from the release commit.
- [ ] Local DMG and ZIP packaging completes and the embedded app reports version `0.4.26` build `30`.
- [ ] The release DMG, ZIP, app signature, and plist pass local verification.
- [ ] `main` is pushed as a fast-forward and annotated tag `v0.4.26` targets the release commit.
- [ ] The tagged GitHub Actions workflow completes signing, notarization, appcast generation, and asset upload.
- [ ] The source release and public Sparkle update release both expose `RelayRunner.dmg`, `RelayRunner.zip`, and `appcast.xml`.

## Run log

- Executing inline on `main`; no worker run was dispatched.
