---
id: RR-100
title: Add GPT-5.6 Codex model choices
status: in_progress
priority: medium
depends_on: [RR-99]
run_id: 287
canceled: false
---

## Description

After RR-99 confirms the current Codex contract, add GPT-5.6 preview models to Relay Runner's Codex model catalog without making them the default.

The intended Codex choices are:

- `gpt-5.6-sol` labeled as GPT-5.6 Sol Preview.
- `gpt-5.6-terra` labeled as GPT-5.6 Terra Preview.
- `gpt-5.6-luna` labeled as GPT-5.6 Luna Preview.

Keep GPT-5.5 as the normal documented default path unless current OpenAI docs make GPT-5.6 generally available and recommend it as the Codex default before implementation starts.

Likely touch points:

- `Sources/relay-runner/Config/Config.swift`
- `Sources/relay-runner/Settings/GeneralSettingsTab.swift`
- `Sources/relay-runner/Onboarding/OnboardingView.swift`
- `services/config.py`
- `tests/RelayRunnerTests/GeneralConfigTests.swift`
- `tests/RelayRunnerTests/ProcessManagerLaunchTests.swift`

## Acceptance criteria

- [ ] Codex model options include GPT-5.6 Sol, Terra, and Luna using confirmed model IDs from RR-99.
- [ ] GPT-5.6 choices appear only for the Codex provider in Settings and onboarding; Claude still shows only Claude-scoped choices.
- [ ] Selecting any GPT-5.6 model persists through app config load/save and Python service config normalization.
- [ ] Start Session passes the selected GPT-5.6 model through the existing Codex `--model` launch path.
- [ ] Invalid cross-provider model selections still reset to `default` when switching between Codex and Claude.
- [ ] Swift and Python tests cover the new Codex model catalog and provider-scoped validation.
- [ ] Provider parity is explicit: Claude model behavior is unchanged, and any intentional Codex-only preview labeling is documented in the implementation notes.
