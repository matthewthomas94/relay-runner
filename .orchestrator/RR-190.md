---
id: RR-190
title: Fix repeat-onboarding visual and permission regressions
status: done
priority: high
depends_on: [RR-189]
run_id: null
canceled: false
---

## Description

Correct three regressions visible when exercising the full first-run experience through `Redo Onboarding…`. The shared message-playing point matrix must remain clipped to the black onboarding/notch surface throughout its reveal and cinematic. The cinematic-to-permission handoff must preserve the hero text baseline and use the same cursor/backspace transition language for permission copy rather than jumping directly to a displaced prompt. Permission grant actions must remain functional on every manual onboarding run, including the second and third run in one app process, without stale permission-coordinator state swallowing the click.

Implement this inline as a continuation of the explicitly requested RR-189 onboarding test work; do not dispatch a worker.

## Acceptance criteria

- [x] The point matrix is clipped to the onboarding/notch surface at every reveal height and never draws over the underlying app or desktop.
- [x] The intro-to-permission transition keeps the text on the established hero baseline.
- [x] Permission copy enters through the cursor/backspace animation instead of jumping or moving upward.
- [x] `Grant permission` starts the appropriate live permission setup on the first, second, and third manual onboarding run in the same process.
- [x] Stale permission setup state is cleared without disrupting the intentional System Settings handoff.
- [x] Focused visual-layout, timeline, and repeat-permission regressions pass along with the full Swift test suite.
- [x] A new DMG and installed app bundle are verified before launch.

## Run log

- Implemented inline on `main` as requested; no worker run was dispatched.
- Added a fixed outer clip around the translated shared message-playing particle field so the point matrix cannot cross the black reveal surface.
- Kept permission copy on the cinematic hero baseline and added cursor hold, backspace, typing, and control reveal phases; the prior prompt is retained across System Settings panel dismissals.
- Clear any stale onboarding-owned permission setup before manual redo and make every recreated onboarding panel key-eligible for reliable button interaction.
- Added coverage for the particle clip, prompt transition and baseline, interactive panel focus, and three full redo cycles with a live permission action each time.
- All 452 Swift tests pass.
- Built and strictly verified `dist/RelayRunner.dmg`, installed a byte-identical executable at `/Applications/Relay Runner.app`, and launched it successfully.
