---
id: RR-150
title: Refresh ChatGPT Codex and Claude model support
status: backlog
priority: medium
depends_on: []
run_id: null
canceled: false
worker_model: strong
worker_effort: high
worker_sizing_rationale: "Model availability now varies by provider, account, CLI build, and selected model; the change spans executable discovery, configuration normalization, launch flags, UI copy, and provider-parity tests."
worker_provider_notes: "Codex 0.144 exposes model-specific max/ultra effort for GPT-5.6. Claude Code 2.1.175 supports Fable and Opus through xhigh/max, Sonnet through max but not xhigh, and no explicit effort for Haiku. Preserve one shared UX while validating provider/model-specific values."
---

## Description

Update Relay Runner's model catalogue and launch compatibility for the current ChatGPT/Codex and Claude Code releases.

The installed ChatGPT app bundles Codex CLI 0.144 under `ChatGPT.app`, with generally available GPT-5.6 Sol, Terra, and Luna plus model-specific `max` and `ultra` effort support. The installed Claude Code 2.1.175 CLI advertises `fable` and `claude-fable-5` in addition to the established `opus`, `sonnet`, and `haiku` aliases. RR-149 added discovery of the current `ChatGPT.app` executable path, but Relay Runner still labels GPT-5.6 as a limited preview, keeps a provider-wide effort list, and offers no Claude Fable choice.

Support only models that the corresponding agent CLI can launch. Consumer-only ChatGPT fallback, voice, and application models must not appear in Relay Runner's Codex session picker unless the Codex CLI exposes them.

Use current primary provider documentation and installed CLI catalogues as the implementation-time source of truth. Record the checked provider and CLI versions in verification notes so future model refreshes can distinguish catalogue drift from regressions.

## Acceptance criteria

- [x] Codex executable discovery supports the current `ChatGPT.app/Contents/Resources/codex` path, the legacy `Codex.app` path, configured absolute commands, and `PATH` fallback in a documented order.
- [ ] The Codex picker reflects current launchable Codex models without adding ChatGPT-only fallback, voice, or application models.
- [ ] The Claude picker includes the current Claude Code aliases and any explicitly supported full model IDs chosen for stable user selection, including the Fable 5 launch path advertised by the installed CLI.
- [ ] Model choices clearly communicate preview, entitlement, or provider restrictions that can cause a syntactically valid model to fail for an account.
- [ ] Reasoning-effort choices are derived from the selected provider and model, including model-specific Codex `max` or `ultra` support without exposing unsupported combinations.
- [ ] Default model selection continues to omit `--model`, preserving the user's account-level provider default.
- [ ] Existing saved model and effort values normalize predictably when a model is retired or an effort is unsupported; switching providers cannot leak the previous provider's model or effort.
- [ ] Codex and Claude launch rendering tests cover every newly exposed model/effort combination and reject unsupported combinations.
- [ ] Settings, onboarding, README model lists, and verification notes agree on the supported catalogue and provider-specific limitations.

## Verified implementation matrix

The implementation must use the following local CLI snapshot rather than guessing model strings:

| Provider model | Picker value | Supported session effort |
| --- | --- | --- |
| Default Codex account model | `default` | `default` |
| GPT-5.6 Sol | `gpt-5.6-sol` | `default`, `low`, `medium`, `high`, `xhigh`, `max`, `ultra` |
| GPT-5.6 Terra | `gpt-5.6-terra` | `default`, `low`, `medium`, `high`, `xhigh`, `max`, `ultra` |
| GPT-5.6 Luna | `gpt-5.6-luna` | `default`, `low`, `medium`, `high`, `xhigh`, `max` |
| GPT-5.5 | `gpt-5.5` | `default`, `low`, `medium`, `high`, `xhigh` |
| GPT-5.4 | `gpt-5.4` | `default`, `low`, `medium`, `high`, `xhigh` |
| GPT-5.4 Mini | `gpt-5.4-mini` | `default`, `low`, `medium`, `high`, `xhigh` |
| GPT-5.3 Codex Spark | `gpt-5.3-codex-spark` | `default`, `low`, `medium`, `high`, `xhigh` |
| Default Claude account model | `default` | `default` |
| Best available Claude | `best` | `default`, `low`, `medium`, `high`, `xhigh`, `max` |
| Claude Fable 5 | `fable` | `default`, `low`, `medium`, `high`, `xhigh`, `max` |
| Claude Opus (latest alias) | `opus` | `default`, `low`, `medium`, `high`, `xhigh`, `max` |
| Claude Sonnet (latest compatible alias) | `sonnet` | `default`, `low`, `medium`, `high`, `max` |
| Claude Haiku (latest alias) | `haiku` | `default` |

The authenticated Codex catalogue is the user-facing authority: it lists GPT-5.3 Codex Spark, while the bundled offline fallback lists GPT-5.2 instead. Keep Spark for this account rather than replacing it from the fallback catalogue. `codex-auto-review` is hidden and must not appear in the session picker. `claude-fable-5` is a valid full model ID, but the picker should use the documented `fable` alias unless a second pinned choice provides a clear user benefit. Include the documented `best` alias because it selects Fable when entitled and otherwise falls back to the latest Opus. Do not expose `opusplan` or 1M-context variants as models in this ticket. Do not pin Claude Sonnet 5: the existing `sonnet` alias remains compatible with Claude Code 2.1.175, while explicit Sonnet 5 requires Claude Code 2.1.197 or later.

## Decisions

- Keep **Default** provider-neutral in behavior: omitting `--model` preserves the account-level provider default.
- Derive Codex effort choices from the selected model. An unknown account Default omits explicit effort; pre-5.6 named models remain capped at `xhigh`; Sol and Terra add `max` and `ultra`; Luna adds `max` only.
- Derive Claude effort choices from the selected model too. Default and Haiku omit explicit effort; Best, Fable, and Opus allow `low` through `max` including `xhigh`; the installed-compatible Sonnet alias allows `low`, `medium`, `high`, and `max` but not `xhigh`.
- When a provider/model change makes the saved effort invalid, normalize the effort to **Default**. When a saved model is retired or belongs to the other provider, normalize the model and effort to **Default**.
- Show concise entitlement copy: GPT-5.6 model and `ultra` availability depend on the Codex plan; Fable availability depends on an eligible Claude plan or usage credits and is unavailable under zero-data-retention arrangements. Do not present provider restrictions as local validation failures.
- Keep sub-agent tier sizing (`fast`, `balanced`, `strong`) provider-neutral. Model-specific session effort does not silently widen ticket `worker_effort` rules in this ticket.

## Source snapshot

- [OpenAI GPT-5.6 general-availability announcement](https://openai.com/index/gpt-5-6/) and the refreshed authenticated catalogue from ChatGPT-bundled Codex `0.144.0-alpha.4` `debug models`, checked 2026-07-12. The bundled offline catalogue was compared separately and is not used to replace account-visible choices.
- [Anthropic Claude Code model configuration](https://code.claude.com/docs/en/model-config), [model-specific effort support](https://platform.claude.com/docs/en/build-with-claude/effort), [Claude Fable 5 availability](https://www.anthropic.com/claude/fable), and installed Claude Code `2.1.175` `--help`, checked 2026-07-12.

## Progress

- 2026-07-12: RR-149 delivered and tested current `ChatGPT.app` Codex executable discovery as a prerequisite for embedded session launch. The model catalogue, compatibility rules, migration behavior, and provider-facing copy remain backlog work.
- 2026-07-12: Refined the existing ticket for dispatch with the installed Codex and Claude catalogues, model-specific effort matrix, provider parity decisions, and current general-availability/entitlement notes. No duplicate ticket was created.
