---
id: RR-325
title: Verify lossless single-effect voice delivery under faults
status: verification_blocked
priority: high
execution_mode: implementation
depends_on: [RR-324]
run_id: 81
canceled: false
verification_blocker: The prior mounted Codex latency result is invalid because it correlated a non-authoritative Messenger handoff rather than the retained authoritative final, so no valid authoritative-final sample set at or below 500 milliseconds p95 or human audible attestation is available. Claude mounted verification is user-deferred pending a subscription.
verification_resume: Collect clean mounted Codex authoritative-final samples with matching acknowledgement, play-request, and utterance identity at or below 500 milliseconds p95, and record matching human audible evidence. Revisit Claude only when a subscription is available.
order: 325
worker_model: strong
worker_effort: high
worker_sizing_rationale: The work needs a multi-process fault matrix and mounted app verification across two providers.
worker_provider_notes: Run equivalent mounted acceptance scenarios for Codex and Claude and document intentional adapter differences.
---
## Description

Build an end-to-end verification matrix for accepted voice turns after the lifecycle broker lands. Exercise process restarts, duplicate callbacks, delayed acknowledgements, recovery, cancellation, and competing outputs, with mounted app coverage for both providers. Bind this ticket to the accepted lifecycle-broker ticket before promotion.

## Acceptance criteria

- [ ] A deterministic fault-injection harness covers messenger, foreground provider, daemon, bridge, and Swift restarts at each lifecycle boundary.
- [ ] Each accepted turn is proven to have exactly one claim, one terminal acknowledgement, and at most one user-visible effect.
- [ ] Cancellation and replacement tests prove that revoked turns cannot emit a late effect.
- [ ] Mounted Codex and Claude runs record correlated identifiers for accepted playback without duplicate effects.
- [ ] Normal-path acknowledgement-to-playback latency is measured and remains at or below 500 milliseconds at p95.
- [ ] Failures produce bounded allowlisted diagnostics that identify the violated lifecycle invariant.

## Provenance

- Originating spike: `RR-321`
- Spike run: 8

<!-- relay-spike-followup-key: 694f5422f29933cf2a04c6397be08d079f9bdb7e85e88f96a302465948d7cb2c -->

## Run log

### Run 36 (attempt 11) — branch `relay/rr-325`

- Preserved the six independently reviewed run-35 commits, then made `turn_revoked` terminal in both real bridge delivery entry points without widening the intentional `dual_write` compatibility fallback.
- Added Codex/Claude integration regressions for cancellation and replacement before reservation across explicit and missing-final delivery; all eight cases assert zero Messenger final, zero TTS effect, and zero broker effect reservation.
- Passed 133 focused Python tests, the focused Swift projection consumer test, and the 70-scenario end-to-end harness (zero violations; 14.643 ms p95 across 56 acknowledgement-to-playback samples).

Implementation is complete. Verification is blocked only on the signed-app authenticated real-audio condition recorded in frontmatter; mounted Codex and Claude evidence remains the required follow-up.

### Mounted verification follow-up — 2026-08-22

- Automated source: the current `main` build passed the 70-scenario provider fault matrix (56 Python scenarios and 14 Swift scenarios), both providers, all 18 revocation/replacement probes, and zero lifecycle violations. The deterministic matrix reported 56 latency samples with 4.318 ms p95.
- Mounted Codex: a Developer ID signed 0.4.48 (build 52) app recorded one unique `provider_acknowledged` and one authoritative final `afplay_started` for command sequence 7, with no duplicate authoritative effect. The reported 3,365.099 ms sequence 10 result is invalid because `speech-latency-report.py` selected a non-authoritative Messenger handoff (`kind=handoff`, `source=messenger`) instead of the later retained authoritative final; it does not establish either a pass or a failure for the mounted <=500 ms gate.
- Mounted Claude: blocked. Claude Code launched, but both the protected manual turn and queued Relay turn ended with `401 OAuth access token has been revoked`; no authenticated final or playback correlation is claimed.
- Provider-scope decision: on 2026-08-22 the user explicitly deferred Claude mounted verification because no Claude subscription is available. This records a temporary waiver, not a Claude pass.
- Physical audio: not attested. `afplay_started` and `played` are process evidence only.

The ticket remains `verification_blocked` because no clean mounted Codex authoritative-final latency sample set or physical audio attestation is available. Claude may be revisited when a subscription is available.

- **Preserved run collision recovery** — historical run 36 (attempt 11); replacement run 81; occupied row retained for RR-338.
