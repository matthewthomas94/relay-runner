---
id: RR-195
title: Cut Relay Runner v0.4.25
status: backlog
priority: high
depends_on: [RR-194]
run_id: null
canceled: false
---

## Description

Prepare and publish the next Relay Runner patch release from the fully integrated `main` branch. Advance the product version from `0.4.24` to `0.4.25` and the monotonic Sparkle build number from `28` to `29`, verify the full Swift and Python suites plus local release packaging, push `main`, create and push the annotated `v0.4.25` tag, then monitor the signed/notarized GitHub Actions release path through source and public Sparkle asset publication.

Execute the release preparation inline on `main`; do not dispatch a worker for the authenticated publication step.

## Acceptance criteria

- [ ] `Info.plist` reports `CFBundleShortVersionString` `0.4.25` and `CFBundleVersion` `29`.
- [ ] The full Swift and Python test suites pass from the release commit.
- [ ] Local DMG and ZIP packaging completes and the embedded app reports version `0.4.25` build `29`.
- [ ] The release DMG, app signature, and plist pass local verification.
- [ ] `main` is pushed as a fast-forward and annotated tag `v0.4.25` targets the same commit.
- [ ] The tagged GitHub Actions release workflow completes signing, notarization, appcast generation, and asset upload.
- [ ] The source release and public Sparkle update release both expose `RelayRunner.dmg`, `RelayRunner.zip`, and `appcast.xml`.

## Run log

- Executing inline on `main`; no worker run was dispatched.
