---
id: RR-160
title: Polish Workspace project header, empty lanes, and session control
status: ready
priority: high
depends_on: []
run_id: null
canceled: false
worker_model: strong
worker_effort: high
worker_sizing_rationale: "The change is localized to the Program Workspace, but exact SwiftUI geometry, typography, empty-state sizing, and visual verification against several Figma sublayers require careful implementation."
worker_provider_notes: "Provider-neutral Workspace UI. Codex and Claude sessions must retain identical project filtering and session-control behavior."
---

## Description

Align the Program Workspace with the latest Figma frame and supplied screenshot, focusing on four visible discrepancies: lane empty states, the Projects column header, Start Session styling, and the Start Session screen-edge clearance.

Design references:

- Figma file: `IGjwpE5m7vLaCThHgoy0cw`
- Root frame: `738:5819` (`Session active - Workspace displaying`)
- Workspace frame: `738:5886`
- Board: `738:10544`
- Projects column: `738:10545`
- Projects header: `839:717`
- Queued empty state: `842:843`
- Start Session control: `846:1670`
- Supplied reference: `/Users/matthewthomas/Downloads/Session active - Workspace displaying.png`

Replace the overview column's separate Status and project-filter rows with the single Figma Projects header. It is positioned inside the existing 8-point panel inset at y=16, measures 299.2 by 32 points at the 1728-point reference width, and uses 16-point internal horizontal padding. The left group contains `Projects` in PP Mori Semibold at 16 points with an adjacent 12-point refresh indicator and a 12-point gap. The trailing `Select all` capsule is 82 by 32 points, uses 12-point horizontal and 8-point vertical padding, a full capsule radius, black fill, the established `#11161d` border token, and disabled/secondary PP Telegraf text at 14 points with a 16-point line height. Remove the visible project count and `All tickets`/selected-scope subtitle from this header without changing project filtering or refresh behavior.

The project list begins at x=8 and y=96 relative to its panel, keeping the existing 8-point lane-aligned card inset, 136-point cards, and 8-point gaps. Preserve scrolling and project selection.

Restyle empty ticket lanes to match node `842:843`: the empty-state body begins at x=8 and y=100, fills the remaining 299.2 by 551-point lane body at the reference size, has no card fill, border, dashed outline, or nested empty-state panel, and centers the existing lane-specific message on both axes. Use PP Telegraf Regular at 14 points, 16-point line height, and the semantic disabled text color matching `#64748b`. Non-empty cards and drop behavior remain unchanged.

Match Start Session to node `846:1670`: 126 by 32 points, 16-point capsule radius, filled dark primary background (the dark-mode token represented by the Figma variable), no extra outline, 12-point horizontal and 8-point vertical padding, 4-point gap, 14-point play icon, and PP Telegraf Regular text at 14 points/16-point line height in the `#f8fafc` neutral token. Preserve hover/cursor, help, accessibility, start action, and the active End Session variant in the same slot and component vocabulary.

At the 1728-point reference width, the Workspace frame begins 20 points from the screen edge and the session control ends 20 points inside that frame. Therefore the toolbar control must end 40 points from the physical screen-right edge. Keep its top at y=6. Do not alter the board's 44-point gutters, five-column geometry, or 667-point height.

Use the existing SwiftUI surface, color, typography, and button primitives. Do not add font or icon assets. Keep Terminal, Settings, ticket CRUD, drag/drop, project selection, session launching, and provider behavior unchanged.

## Acceptance criteria

- [ ] The overview header reads `Projects` and no longer renders `Status`, the project count, or the scope subtitle.
- [ ] The Projects header retains the working refresh control and `Select all` action, with Figma node `839:717` geometry and styling: 32-point height, 16-point header inset, 16-point PP Mori heading, 12-point refresh icon/gap, and 82×32 trailing capsule with 14-point PP Telegraf text.
- [ ] Project cards begin at x=8 and y=96 relative to the panel at the reference size, retaining 136-point height, 8-point gaps, scrolling, selection, and the lane-aligned horizontal inset from commit `4f227ea`.
- [ ] Every empty work lane fills the body from y=100 to the panel's bottom inset, centers its lane-specific copy horizontally and vertically, and has no fill, border, dashed outline, or nested empty-state card.
- [ ] Empty-state copy uses PP Telegraf Regular at 14 points/16-point line height and semantic disabled color matching `#64748b`.
- [ ] Start Session matches node `846:1670`: 126×32 points, 16-point radius, filled dark primary background without an extra outline, 12/8 padding, 4-point gap, 14-point icon, and 14-point PP Telegraf text in `#f8fafc`.
- [ ] The session control top remains y=6 and its right edge is 40 points from the physical screen edge on the 1728-point reference frame (20 points inside the Workspace frame).
- [ ] Start Session still invokes the existing action; active sessions expose End Session in the same slot with equivalent interaction, accessibility, and provider-neutral behavior.
- [ ] Board gutters, column sizing, non-empty cards, project filtering, drag/drop, ticket CRUD, Terminal, and Settings do not regress.
- [ ] Focused tests cover Projects-header presentation, empty-state styling/geometry, Start/End presentation, and the 40-point screen-right toolbar inset.
- [ ] `swift test` and `git diff --check` pass.
- [ ] Visual verification compares the implementation against the 1728×1117 screenshot for Figma node `738:5819`.
