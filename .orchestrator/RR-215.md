---
id: RR-215
title: Rebuild and install the current Relay Runner app
status: done
priority: high
depends_on: []
run_id: 321
canceled: false
worker_model: balanced
worker_effort: low
worker_sizing_rationale: "This is a routine source-of-truth packaging and local installation task with little implementation ambiguity but a long build and explicit artifact verification."
worker_provider_notes: "none"
---

## Description

Build the current `main` revision through the supported release packaging path and refresh the local `/Applications/Relay Runner.app` installation. This is an operational build/install task, not authorization to modify source code or packaging scripts.

## Acceptance criteria

- [x] Confirm the repository is on `main` with a clean tracked worktree and record the source commit being packaged.
- [x] Run `./scripts/build-dmg.sh` successfully without modifying tracked source files.
- [x] Verify `dist/Relay Runner.app`, `dist/RelayRunner.dmg`, and the generated Sparkle zip exist and are non-empty.
- [x] Ensure `/Applications/Relay Runner.app` is the newly built app, using the build script's supported refresh path when an installed copy already exists.
- [x] Verify the installed bundle's identifier, executable presence, code-signature validation, and executable checksum against `dist/Relay Runner.app`.
- [x] Do not quit or relaunch the currently running Relay Runner process as part of this ticket.

## Run log

**Run 321** (attempt 4) - branch `relay/rr-215`

- Claimed the ticket, then rebuilt via `./scripts/build-dmg.sh`; the script completed successfully and refreshed `/Applications/Relay Runner.app` through the supported install path.
- Verified `dist/Relay Runner.app`, `dist/RelayRunner.dmg`, and `dist/RelayRunner.zip` exist and are non-empty; verified `/Applications/Relay Runner.app` has bundle id `com.relayrunner.app`, an executable at `Contents/MacOS/relay-runner`, and passes `codesign --verify --deep --strict`.
- Verified the installed executable SHA-256 matches `dist/Relay Runner.app/Contents/MacOS/relay-runner`; packaged app content corresponds to `main` commit `dfe9ce5`, with only RR-215 ticket metadata committed on top in this worker branch.

Unresolved questions or follow-ups: none.

Result: done.
