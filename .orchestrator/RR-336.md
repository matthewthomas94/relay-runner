---
id: RR-336
title: Verify autonomous voice continuity recovery under faults
status: done
priority: high
execution_mode: implementation
depends_on: [RR-335]
run_id: 71
canceled: false
order: 336
worker_model: strong
worker_effort: high
worker_sizing_rationale: End-to-end fault verification spans real audio, multiple processes, two providers, duplicate-effect safety, privacy, and external mounted-app evidence.
worker_provider_notes: Exercise equivalent Codex and Claude recovery behavior and report provider-specific limitations without weakening the shared evidence standard.
---
## Description

Build deterministic and mounted verification for the complete RR-332 through RR-335 continuity design. Prove that Relay Runner can detect a broken speech-to-work pipeline, launch an isolated continuity agent, recover Relay-owned runtime processing without unrelated side effects, and safely resume or request repetition. Persist a privacy-safe incident report and refined permanent-fix proposals when the core defect remains.

Automated evidence and signed mounted-app evidence must be recorded separately. The ticket may remain verification-blocked when authenticated real-provider or physical audio evidence is unavailable; it must not substitute worker exit, unit tests, or simulated playback for that external proof.

## Acceptance criteria

- [x] A deterministic fault harness covers transcription dropout, capture interruption, bridge loss, messenger crash, foreground Codex and Claude hang or exit, daemon or IPC loss, stale session ownership, configured provider fallback, simultaneous component faults, and recovery-budget exhaustion.
- [x] For recoverable cases, evidence shows incident classification, continuity-agent launch, broker actions, restored component health, canonical foreground handoff, and continued work without manual session restart.
- [x] Verification proves no duplicate command execution, no duplicate TTS, no stale reply, no continuity-agent speech, no unauthorized project mutation, and no credential, transcript, repository, screenshot, raw-log, or external-data exposure.
- [x] Pre-transcript speech loss produces a healthy listening state and an explicit repeat request; captured recoverable commands retain exact command and intent identity.
- [x] A signed mounted-app lane correlates real speech capture, transcript or repeat outcome, provider acknowledgement, command lifecycle, and audio playback identifiers for both configured providers where the environment permits.
- [x] Unresolved incidents persist a sanitized structured report and editable permanent-fix proposals; proposals require individual PM or user acceptance, enter Backlog, and never auto-dispatch.
- [x] The run log separates automated source evidence, mounted app evidence, physical audio evidence, and any exact verification blocker or resume condition.

## Run log

**Run 71** (attempt 7) — branch `relay/rr-336`

- Automated source: cherry-picked reviewed commits `a46aa30a` and `95a0770c`; tightened the mounted gate to require one exact production-shaped recovery chain per Codex and Claude scenario, with negative coverage for healthy non-recovery, mixed generation/provider, and mismatched audible evidence. All 98 continuity tests passed on Homebrew Python 3.14, and the 14-case fault matrix passed every event-backed safety invariant.
- Mounted app: not performed in this isolated worktree; no authenticated signed mounted-app recovery capture was available. Source verification does not establish the Developer ID mounted build or real-provider behavior.
- Physical audio: not performed; no human audible-playback attestation was available. Implementation is complete and the ticket is `verification_blocked` until the frontmatter resume condition is completed; no unresolved code question remains.

### Mounted verification follow-up — 2026-08-22

- Automated source: the current `main` build passed 71 continuity/recovery Python tests and the 14-case continuity fault harness for Codex and Claude with no duplicate command, TTS, stale-reply, privacy, or project-mutation violations.
- Mounted app: the current Developer ID signed app was installed and exercised, but no production continuity JSONL containing the required recovered chain exists. A fault-recovery run was not represented by ordinary healthy-session evidence.
- Provider availability: Codex was authenticated; Claude launched but returned `401 OAuth access token has been revoked`, so an authenticated Claude recovery scenario could not complete.
- Provider-scope decision: on 2026-08-22 the user explicitly deferred Claude mounted verification because no Claude subscription is available. This is not represented as a pass.
- Physical audio: no human audible-playback attestation was available, and the mounted verifier was not run with `--physical-audio-attested`.

The ticket remains `verification_blocked` on a complete Codex production-shaped recovery chain, 60 seconds of restored stable health, correlated playback, and matching human audible attestation. Claude is deferred until a subscription is available.

### Mounted verifier scope repair — 2026-08-26

- Added an explicit `--defer-provider PROVIDER=REASON` scope decision. With no flag, Codex and Claude remain required. A deferred provider is reported as `deferred` with its reason and is never represented as passed; at least one supported provider must still complete the mounted lane.
- Empty reasons, unsupported providers, duplicate CLI entries, and attempts to defer every provider fail or remain verification-blocked.
- Source verification: 105 continuity tests and 4 provider-turn fault tests passed, the 14-case fault matrix passed every invariant, Python compilation passed, and `git diff --check` passed.
- Mounted app and physical audio remain unverified by this repair. Resume with the exact Codex recovery scenario and matching human audible attestation above; run the verifier with `--physical-audio-attested --defer-provider 'claude=User deferred: no active subscription.'`.

### Inline mounted-recovery repair — 2026-08-26

- Live fault reproduction: the signed mounted app classified an exact Relay-owned Codex provider stall and autonomously replaced the stopped embedded provider while preserving the app and voice bridge. The recovery lane nevertheless ended `circuit_open`: replacement readiness was not objective recovery evidence, the required 60-second stable-health window was not enforced after an early agent finish, and the interrupted acknowledged command was correctly not replayed.
- Runtime repair: embedded Codex and Claude replacements now publish a privacy-safe `provider_ready` event only when the native command, opaque command, provider, and recovery generation still match. Interactive readiness or observed provider progress satisfies the provider-processing-ready postcondition; neither event creates a synthetic provider-turn deadline.
- Stable-health repair: after any applied recovery, an early continuity-agent finish forces `check_processing_health`. Fresh restored evidence below the configured threshold waits interruptibly for the remaining stable window and is checked again within the existing attempt and wall-clock circuit breakers.
- Source verification: 107 continuity tests, 4 provider-turn fault tests, the 14-case safety matrix, and 801 Swift tests passed; Python compilation and `git diff --check` passed. Provider parity is covered for Codex and Claude.
- Mounted candidate: the Developer ID signed DMG was rebuilt with `/Applications/Relay Runner.app` explicitly preserved, mounted at `/tmp/rr336-fixed-mounted`, and its app passed strict deep signature verification. This candidate is signed but not notarized and has not yet completed the fresh live recovery/audio scenario.

The ticket remains `verification_blocked` until the new mounted candidate demonstrates the complete Codex recovery chain, 60 seconds of restored health, same-session continued work, correlated authoritative playback, and matching human audible attestation. The interrupted acknowledged command remains fail-closed and must not be replayed.

### Signed mounted verification and closure — 2026-08-26

- Final runtime repairs serialize cross-process authorization-ledger and private command-journal rewrites, and queue a distinct continuity incident that arrives during the single-flight recovery window. The simultaneous-fault harness now requires both incidents to recover serially instead of accepting a silently dropped second incident.
- Automated source: 109 continuity tests, 128 spike-follow-up and voice-preemption tests, 4 provider-turn fault tests, and the 14-case Codex/Claude continuity harness passed. The harness reported zero duplicate command execution, duplicate TTS, stale reply, continuity-agent speech, unauthorized mutation, or privacy violations. Python compilation and `git diff --check` passed. The same Swift runtime source passed 801 tests before the external-verifier-only follow-up; no Swift source changed afterward.
- Artifact: the final DMG (`269ef5e535db29f8846d56f9fd3cf4055e2d2aa3f9998103db6ac21c3e4bd4dd`) and zip (`3530eebde04a235772e5878f8557a56df776050227e119a04d6bdfa4c683f978`) were built with `/Applications/Relay Runner.app` explicitly preserved. The DMG was mounted read-only at `/private/tmp/rr336-mounted-v2.42WMwt`; the mounted app passed host-level strict deep Developer ID verification for team `QK9K4AQRNH`. This local candidate is signed but not notarized.
- Mounted recovery: Codex command sequence 13 (`90772-1787747519378349000-13`) produced stalled incident `inc-49f47104f857`. The app process and voice bridge remained alive while the continuity agent replaced the stopped embedded provider, proved `provider_process_alive` and `provider_processing_ready` for 60.001 seconds, completed `restored`, and selected canonical `reattach`. The acknowledged interrupted command remained terminated and was not replayed.
- Continued work: follow-up sequence 14 (`90772-1787748161581613000-14`) was captured in the same mounted app, provider-acknowledged, and completed as an authoritative final. The linked verifier matched play request `8c081e3c-c122-4876-b593-8540f85e7640` and utterance `a5a258e4-b328-4a60-b283-d30f4fbfcb56` to one `afplay_started` event. The user triggered queued playback with double-Option and explicitly attested hearing the expected reply; double-Control is dismissal and was not counted as playback evidence.
- The mounted verifier passed with no blocker codes and reported Developer ID signature, Codex recovery, command lifecycle, correlated playback, and physical audio as passed. Claude mounted verification remains explicitly deferred because no active subscription is available; it is reported as `deferred`, never passed.
- Canonical ticket status is `done`. Preserved run 71 remains `VerificationBlocked` as historical audit truth for the original worker outcome; the documented ledger disagreement is intentional.
