---
id: RR-75
title: Top-align Program Board ticket lanes
status: done
priority: medium
depends_on: []
run_id: 65
canceled: false
---

## Description

On the Program Board, ticket cards currently appear vertically centered within
their lane when the lane has only a few cards. Lanes should populate from the
top down, matching expected kanban behavior and the project-board layout.

Likely areas to inspect:

- `Sources/relay-runner/Board/ProgramBoardOverlayView.swift`
- `Sources/relay-runner/Board/BoardOverlayScrollView.swift`

The captured command suggested checking the live Program Board via Relay Vision.
The foreground session attempted that, but `relay-vision/screenshot` returned a
closed transport error, so this ticket is based on the user report plus the
Program Board layout code.

## Relay command

- sequence: 1
- id: 69741-1781253485472087000-1
- Newer Relay commands supersede stale ticket edits and dispatches.

## Acceptance criteria

- [ ] Program Board ticket lanes render cards starting at the top of the lane content area when the lane has fewer cards than the available vertical space.
- [ ] Empty-lane messaging remains top-aligned with the lane content area rather than floating in the center.
- [ ] The fix preserves scrolling behavior for lanes with many tickets.
- [ ] Project-board columns are not regressed; if shared scroll layout code changes, verify the single-project board still top-aligns cards and still scrolls.
- [ ] Add or update focused tests where practical; otherwise document a manual visual verification path for the Program Board layout.

## Run log

### Run 65 (attempt 1) - branch `relay/rr-75`

- Updated `BoardOverlayScrollContainer` so the scroll document can fill the viewport while hosted SwiftUI content keeps its measured height at the top edge.
- Added focused AppKit layout tests covering sparse top-aligned content and tall scrollable content in the shared board scroll container.
- Verified with `swift test --filter BoardOverlayScrollViewTests` and full `swift test` (125 XCTest tests, 0 failures). Done.
