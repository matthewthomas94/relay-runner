---
id: RR-156
title: Prepare Relay Runner v0.4.21
status: ready
priority: high
depends_on: []
run_id: null
canceled: false
worker_model: balanced
worker_effort: medium
worker_sizing_rationale: "The code change is localized, but release version monotonicity and the full automated preflight require careful verification."
worker_provider_notes: "Provider-neutral release preparation. Codex and Claude behavior must remain unchanged."
---

## Description

Prepare the current `main` branch for production release `v0.4.21`. Update `Info.plist` so `CFBundleShortVersionString` is `0.4.21` and increment the Sparkle-monotonic `CFBundleVersion` from `24` to `25`.

Do not create or push the Git tag and do not publish GitHub releases from the worker. The foreground orchestrator will perform those external release actions after review merges this preparation.

Keep the change surgical. Existing releases are asset-only and use an empty release body, so no changelog or release-note document is required.

## Acceptance criteria

- [ ] `Info.plist` contains `CFBundleShortVersionString` `0.4.21` and `CFBundleVersion` `25`.
- [ ] `plutil -lint Info.plist` passes.
- [ ] `bash -n scripts/build-dmg.sh` and `bash -n scripts/generate-appcast.sh` pass.
- [ ] `.github/workflows/build-dmg.yml` parses successfully with Ruby Psych.
- [ ] `swift test` passes.
- [ ] `python3 -m unittest discover -s tests -p 'test_*.py'` passes.
- [ ] `python3 scripts/build-instructions --check` passes.
- [ ] No tag, push, GitHub release, local DMG build, or installed-app replacement is performed by the worker.
