---
id: RR-53
title: Claude parity for workspace rollout
status: done
priority: high
depends_on: [RR-51, RR-52]
run_id: 38
canceled: false
---

## Description

Add a dedicated Claude parity pass for the workspace-folder rollout and Relay-message steering changes.

The workspace tickets include provider parity in each implementation step, but this ticket should verify the complete Claude experience end to end. Claude should respect the same workspace root versus project repo boundary as Codex, avoid parent `.orchestrator/` creation during workspace discovery, route boards correctly, preserve Program Manager capture metadata, and handle immediate Relay-message steering according to RR-52.

If Claude must differ from Codex because of provider-specific command, auth, cwd, or interruption constraints, document the difference in product-facing terms and make sure the user experience remains deliberate rather than accidental.

## Acceptance criteria

- [ ] Claude Start Session from a workspace root launches with the expected cwd and discovers child projects without creating a parent `.orchestrator/`.
- [ ] Claude Start Session from a single project repo activates that repo and opens the project board behavior.
- [ ] Claude session metadata, Program Manager capture, and Graphify ingestion identify Claude provider context correctly.
- [ ] Claude board routing matches Codex for workspace roots, single repos, and ambiguous parent repo plus child repo folders.
- [ ] Claude Relay-message steering matches RR-52 or documents any provider limitation and user-facing fallback.
- [ ] The verification report compares Claude and Codex behavior side by side for workspace activation, board routing, capture, and interruption.
- [ ] Any Claude-specific auth, command, or environment setup required for the rollout is documented and tested where feasible.

## Run log

**Run 38** (attempt 1) - branch `relay/rr-53`

- Added `docs/verification/RR-53-claude-workspace-parity.md` with side-by-side Codex/Claude verification for workspace activation, board routing, Program Manager capture, Graphify ingestion/status, Relay-message steering, and intentional Claude command/auth/environment differences.
- Added `SessionCaptureTests.test_capture_normalizes_claude_provider_metadata` so native Program Manager capture explicitly verifies Claude provider normalization into Graphify.
- Verified with `python3 -m unittest tests.test_session_capture`, `python3 -m unittest discover tests`, `swift test`, `scripts/build-instructions --check`, and `bash -n scripts/relay-bridge`.

Unresolved questions or follow-ups: none beyond the report's residual risks around live macOS/Claude UI launch, fixture-based workspace tests, and nested workspace discovery remaining out of scope.

Status: done.
