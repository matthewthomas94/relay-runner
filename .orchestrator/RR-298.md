---
id: RR-298
title: Preserve full lifecycle detail in response pills
status: in_progress
priority: high
execution_mode: implementation
depends_on: []
run_id: 18
canceled: false
order: 298
worker_model: codex:sol
worker_effort: xhigh
worker_sizing_rationale: Inherited provider, model, and effort from Relay Runner General Settings.
worker_provider_notes: Use my defaults preserves explicit stable provider selections; Codex resolves Sol/Terra/Luna then uses model_reasoning_effort and Claude uses --effort.
---

## Description

Fix lifecycle outcome text being irreversibly truncated before it reaches the response pill. A mounted `run-verification-resumed` update for RR-279 rendered as `RR-279 verification resumed: Mounted test on the currently running installed Relay Runner v0....` under **Double tap Option to play**, even though the response pill already supports wrapped, scrollable long bodies.

The truncation happens upstream: `OrchestrationTraceEvent` applies the 96-character public trace limit to the only copy of the lifecycle message, then the daemon persists and delivers that shortened value to Messenger. Preserve the intentionally compact status/notch representation while carrying the complete public lifecycle detail through daemon persistence, Messenger realization, and the TTS display-preview path. Do not solve this by merely increasing the visual pill height or by removing privacy checks from public trace content.

## Acceptance criteria

- [ ] Protected lifecycle outcomes, including verification resumed/blocked, failures, cancellations, reconciliation, merges, and success, retain their complete public detail for Messenger and the TTS display preview when the detail exceeds 96 characters.
- [ ] The **Double tap Option to play** pill receives the complete lifecycle detail and uses its existing wrapping, teleprompter, and manual-scrolling behavior instead of displaying an upstream ellipsis.
- [ ] Compact working/notch status text remains bounded and does not expand to the full lifecycle explanation.
- [ ] Ordinary planning/progress traces remain concise; only lifecycle detail that the user needs to understand an outcome uses the complete-detail path.
- [ ] Public-content validation still rejects raw commands, hidden reasoning, transcripts, secrets, credentials, and other private activity before either the compact summary or complete detail is emitted.
- [ ] Daemon persistence and delayed outcome delivery preserve the complete detail; reconnecting the bridge must not fall back to the shortened summary.
- [ ] Messenger realization uses the complete lifecycle detail while retaining existing deduplication, protected-lifecycle priority, stale-command behavior, and authoritative-final arbitration.
- [ ] Codex and Claude app-owned sessions have the same complete-detail behavior.
- [ ] Focused Python regressions cover the trace contract, daemon lifecycle payload/persistence, pending-outcome delivery, and Messenger input using a lifecycle message longer than 96 characters while asserting compact status remains bounded.
