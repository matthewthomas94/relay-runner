---
id: RR-356
artifact_id: ticket-8a924304ddd356896a83677adbf077d2b00cf2e9
user_edited_at: 2026-09-19T04:48:25.813907Z
title: Present the startup primer once per voice session
status: backlog
priority: high
execution_mode: implementation
depends_on: []
run_id: null
canceled: false
order: 356
worker_model: balanced
worker_effort: high
worker_sizing_rationale: Bounded startup presentation bug across Swift readiness and Python speech delivery with existing regression coverage.
worker_provider_notes: Keep identical startup behavior for Codex and Claude; preserve manual bridge launches and tutorial greeting suppression.
---
## Description

Normal app-started voice sessions can display the startup primer twice on screen. The bridge queues STARTUP_GREETING through _queue_tts_text in services/voice_bridge.py; tts_worker publishes its message_waiting preview. Independently AppState.surfaceSessionReadyIfNeeded calls StateMachine.showSessionReady, and OverlayController renders the same greeting. The user confirmed visual duplication, not repeated audio. The live September 19 session had a single greeting utterance accepted and queued at 14:34:36 local time, so downstream repeated playback is not established.

Choose the smallest coherent change giving the startup primer a single presentation owner. Preserve automatic speech when configured, manual playback and replay, normal response previews, and tutorial suppression. Do not silently suppress the first real messenger reply as a side effect of reusing the tutorial greeting flag. Consider greeting-first and readiness-first event ordering, recovery, and subsequent sessions. Avoid unrelated UI or voice changes.

The PM is separately handling microphone recovery. The previously running app executable differed from the installed bundle; relaunch restored screen capture and started fresh microphone capture. Do not change microphone capture, permissions, installed app, or user configuration in this worker.

## Acceptance criteria

- Add a focused regression that exposes the duplicate visual startup paths or their ordering, then verify the startup primer appears through one presentation path per normal session.
- Preserve expected auto-play and explicit playback behavior without dropping ordinary assistant responses or the first user-turn messenger reply.
- Preserve tutorial startup suppression and manual/external bridge behavior for both Codex and Claude; document any intentional difference.
- Run affected Swift/Python tests and git diff --check; distinguish source-test evidence from live installed-app verification, which the PM handles.
- Commit the focused implementation and structured worker result. Do not rebuild/install, restart live services, change other tickets, or push branches.
