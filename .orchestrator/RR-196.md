---
id: RR-196
title: Polish onboarding completion and transition motion
status: backlog
priority: high
depends_on: [RR-195]
run_id: null
canceled: false
---

## Description

Finish the first-run and manual-redo onboarding treatment after workspace selection. Once a folder is successfully chosen and persisted, replace the workspace picker with a centered `You’re all set /` state, preserve the opacity-only blinking slash, then hand off to the real Workspace view through its existing translate-plus-opacity reveal path. Do not duplicate the Workspace reveal animation inside onboarding or momentarily expose the underlying app shell between surfaces.

At the same time, refine the shared onboarding motion and controls to match the supplied reference treatment: make typing and backspacing another 50% faster than the current RR-194 timing, apply ease-in-out timing to text phase transitions, ease the message-playing particle matrix in and out instead of switching it abruptly, keep the workspace path field exactly the same visible height as Browse, and remove hover scaling from every onboarding action button while retaining a restrained color/tone response.

The completion and Workspace handoff must behave identically for a pristine first launch, an interrupted/resumed first run, and Settings → Redo Onboarding. Preserve reduced-motion behavior, workspace-picker cancellation recovery, onboarding flag semantics, and provider parity: Codex and Claude use the same visual completion and handoff.

Reference images:

- `/Users/matthewthomas/Downloads/Session active - Onboarding Welcome.jpg` — centered `You’re all set /` completion treatment.
- `/Users/matthewthomas/Downloads/Session active - Workspace displaying.png` — Workspace view that should reveal after completion.

## Acceptance criteria

- [ ] A successful workspace choice persists the path, then presents `You’re all set /` within the existing onboarding surface before onboarding is marked complete and dismissed.
- [ ] The completion slash continues the existing opacity-only blink and never changes layout width or nudges the centered title.
- [ ] After a short completion hold, onboarding unlocks the app shell and opens the actual Workspace work view via the existing `BoardRevealTransition` translate-plus-opacity path; no duplicate reveal implementation, intermediate Settings/Terminal view, or one-frame shell flash is introduced.
- [ ] The same completion and Workspace handoff runs for first launch, interrupted/resumed onboarding, and manual Redo Onboarding; canceling the folder picker restores the workspace prompt without completing onboarding.
- [ ] Typing and backspacing intervals are another 1.5× faster than the current values (`0.065 / 2.25` and `0.045 / 2.25`, or behaviorally equivalent) and shared text transition phase progress uses ease-in-out timing without breaking centered backspacing.
- [ ] The intro particle matrix remains the exact message-playing renderer/treatment and eases both its appearance and disappearance; it does not hard-toggle, move, or escape the onboarding surface.
- [ ] The workspace path field and Browse button have exactly equal visible heights through one shared dimension.
- [ ] Onboarding buttons do not change scale or footprint on hover. Hover feedback is limited to a color/tone/border/shadow transition using ease-in-out timing.
- [ ] Reduced Motion produces an immediate, stable completion/handoff without unnecessary translation while preserving state changes and opening Workspace.
- [ ] Focused onboarding tests cover completion ordering, workspace open callback, cancellation, faster timing/easing, particle opacity ramps, equal control heights, and the absence of hover scale; the full Swift test suite passes.
