---
id: RR-98
title: Add a Workspace pull-down and typewriter intro to first-run onboarding
status: backlog
priority: high
depends_on: []
run_id: null
canceled: false
worker_model: strong
worker_effort: high
worker_sizing_rationale: "This first-launch window-lifecycle and animation change must reuse the Workspace reveal safely while preserving onboarding recovery, accessibility, and every existing setup path."
worker_provider_notes: "The visual and lifecycle behavior must be identical for Codex and Claude; preserve each provider's existing model, effort, authentication, and launch semantics."
---

## Description

Replace the abrupt titled-window entrance on a genuinely fresh Relay Runner launch with the supplied Workspace-style onboarding intro. Reuse the existing notch-to-Workspace reveal primitive rather than building a second approximation: a transient overlay expands from the compact top surface to full width, pulls down into the same black rounded Workspace backdrop, presents a centered cursor-like type animation, retracts completely, and only then constructs/shows the existing key-capable interactive onboarding window.

The supplied frames establish the visual sequence: a white slash cursor on black, type `First thing’s first /`, erase back to `/`, type `I need a few permissions /`, erase, type `Relay Runner /`, then erase through the supplied `R/` frame back to `/` before retracting. Add a shared `onboardingHero` typography role at roughly 56 points using PP Telegraf Regular (with the normal system fallback) rather than an ad hoc font, and keep the Workspace bottom-corner geometry, screen margin, notch placement, and four-dot visual language. Reserve the active phrase's completed width while typing so partial text grows from a stable leading edge while the completed phrase remains centered. The notch/status label for this phase is `Getting started`.

Keep this cinematic narrowly scoped to the automatic, brand-new first launch. Simplified setup recovery, interrupted-flow resume, upgrade/provider-choice recovery, the "no first session yet" reminder, and Settings' manual `Re-run Setup Walkthrough…` action must go directly to the relevant interactive content without replaying several seconds of animation. Write the `.onboarding-started` sentinel before the animation begins so terminating during it resumes safely.

The existing onboarding remains authoritative after the intro: the cinematic replaces its redundant Welcome step and hands the fresh flow to Agent Choice, while preserving provider selection, model and effort normalization, workspace-folder confirmation, microphone/Input Monitoring setup, app-hosted Accessibility/Screen Recording guidance, Python installation, provider authentication, Ready/Start Session behavior, skip paths, and resume persistence. Do not swap the form into the nonactivating reveal panel: its controls and pickers need the current key-window semantics. Do not route the intro through `ProgramBoardOverlayController`, whose project loading and session state are unrelated; reuse the lower-level reveal planner/container or extract only a small shared primitive. Trigger the typing timeline only from the reveal completion callback so it never plays invisibly behind the opening animation. Add an AppState-owned onboarding-notch override with higher precedence than normal one-second session/activity planning; render setup labels using the persistent `.notWorking` four-white-dot presentation, and clear the override on finish or manual close.

## Acceptance criteria

- [ ] A genuinely fresh automatic first launch expands from the current display's notch/top-center into the same full-width, approximately 726-point black surface and 24-point rounded lower corners used by Workspace, clamped for smaller displays.
- [ ] After the reveal completes, the centered sequence types, holds, and fully erases `First thing’s first`, `I need a few permissions`, and `Relay Runner`, using `/` as the empty and trailing cursor state; the animation is deterministic, maintains stable phrase alignment, and does not start while content is still hidden.
- [ ] The intro phase exposes persistent `Getting started` copy through an AppState-owned `.notWorking` notch override that cannot be overwritten by activity polling, preserving the four-white-dot Relay treatment on notched and non-notched displays and restoring normal planning when cleared.
- [ ] Completion retracts and removes the transient overlay before constructing/showing the existing key-capable onboarding window, starts the fresh interactive flow at Agent Choice (not a duplicate Welcome), and produces no overlap, lost focus, or pre-intro flash.
- [ ] Completing or closing onboarding retracts/cleans up the transient surface, restores the app's normal accessory activation policy, and leaves no timer, panel, or event monitor alive.
- [ ] The intro only plays for the automatic brand-new full flow; simplified/recovery/resume/upgrade/no-session and manual re-run flows skip it while retaining their current initial-step decisions.
- [ ] Reduce Motion bypasses the transient reveal entirely and presents interactive onboarding immediately; VoiceOver receives a stable introductory label instead of every partial string.
- [ ] Multi-display placement uses one resolved display consistently for the panel, reveal plan, and notch geometry, including the top-center fallback when no hardware notch is exposed.
- [ ] Codex and Claude onboarding paths retain equivalent visuals and their existing provider-specific model, effort, sign-in, and launch behavior.
- [ ] Focused tests cover intro eligibility, timeline/frame progression, Reduce Motion, reveal completion ordering, and cleanup; the complete Swift test suite passes.
