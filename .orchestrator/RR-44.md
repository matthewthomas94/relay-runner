---
id: RR-44
title: Deprecate PM-sync guidance and metadata
status: done
priority: medium
depends_on: [RR-43]
run_id: 29
canceled: false
---

## Description

Cleanly deprecate PM-sync from Relay Runner-facing guidance after native session review capture exists.

The user wants a clean cut from PM-sync as a separate ledger/chat workflow. This ticket should remove or replace repo/global guidance that tells agents to run `/pm-sync`, stop relying on `.pm/project-id`, and document the new Program Manager capture flow.

Do not remove historical `.pm/` data from user repos unless the new docs explicitly provide a separate manual cleanup path. This ticket is about Relay Runner guidance and behavior.

Provider parity matters: both Codex and Claude instructions should point to the native program capture flow after this lands.

## Acceptance criteria

- [x] Relay Runner docs/instructions no longer direct users to run `/pm-sync` for program tracking.
- [x] Any `.pm/project-id` dependency in Relay Runner-facing workflows is removed or explicitly marked legacy.
- [x] New docs point users to native session review capture from RR-43.
- [x] Codex and Claude-facing instruction paths are both considered and updated where repo-owned.
- [x] Existing repo-local `.pm/` directories are not deleted automatically.
- [x] Tests or documentation verification confirm no repo-owned PM-sync guidance remains.

## Run log

**Run 29** (attempt 1) — branch `relay/rr-44`

- Added provider-neutral Program Manager capture guidance for Codex, Claude, and relay-orchestrator MCP instructions, all pointing to native `session_capture`.
- Updated orchestrator docs and the Swift `session_capture` tool copy to mark PM-sync and `.pm/project-id` as legacy while preserving existing `.pm/` directories.
- Verified with `scripts/build-instructions --check`, targeted deprecation `grep`, `python3 -m unittest tests.test_session_capture`, and `swift test`.

Unresolved questions or follow-ups: none.

Status: done.
