---
id: RR-104
title: Enforce worker sizing at dispatch
status: done
priority: high
depends_on: [RR-103]
run_id: 289
canceled: false
worker_model: strong
worker_effort: high
worker_sizing_rationale: "Cross-provider dispatch enforcement touches daemon launch, board/status surfaces, and dependency auto-dispatch tests."
worker_provider_notes: "Codex uses model_reasoning_effort; Claude uses --effort. The sizing requirement should be shared even though launch flags differ."
---

## Description

Wire the worker-sizing metadata from RR-103 into the actual dispatch path so tickets cannot be sent to a sub-agent without a deliberate model/effort choice.

Relay Runner should reject or hold `ready` tickets that lack the required sizing metadata, with an actionable reason on the board/log instead of silently starting a default worker. When metadata is present, the daemon should apply it to the worker command using the verified provider-specific mechanism from RR-102.

Likely touch points:

- `services/orchestrator.py`
- `services/program_status.py`
- `Sources/relay-runner/Board/Ticket.swift`
- `Sources/relay-runner/Board/ProgramBoardStatus.swift`
- `tests/test_orchestrator.py` or nearby Python orchestrator tests
- `tests/RelayRunnerTests/*Board*` for visible skipped/held state if the UI exposes it

## Acceptance criteria

- [x] `dispatch_ticket` and the ready-ticket sweeper refuse to dispatch a `ready` ticket missing required worker-sizing metadata.
- [x] The refusal reason is visible through existing run/skip/status surfaces so the user can refine the ticket instead of wondering why nothing happened.
- [x] Codex workers receive the selected model and reasoning effort using the verified `codex exec` command shape.
- [x] Claude workers receive the verified model/tier control where supported, or the daemon records a documented provider limitation while preserving the required sizing rationale.
- [x] Run metadata captures the selected provider, model/tier, effort, and sizing rationale for Program Board/status visibility.
- [x] Tests cover missing metadata, valid Codex metadata, valid Claude metadata or documented omission, and dependency auto-dispatch behavior.
- [x] Provider parity is explicit: Codex and Claude dispatch behavior share the same sizing requirement even when their launch flags differ.

## Run log

- **Run 289** (attempt 1) - branch `relay/rr-104`
  - Added daemon-side worker sizing validation before worktree creation, with failed-run metadata for refused dispatches and provider-specific Codex/Claude launch flags.
  - Passed sizing metadata through run storage, runs-index, Graphify ingestion, Program Status responses, and Program Board detail rows; ready tickets missing sizing now expose an actionable `last_error`.
  - Verified with `python3 -m unittest discover tests -p 'test_*.py'` and `swift test`; no unresolved follow-ups.
  - Status: done.
