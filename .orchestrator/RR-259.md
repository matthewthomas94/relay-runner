---
id: RR-259
title: Restore real-pointer hover and click routing on the first Workspace card
status: done
priority: high
depends_on: []
run_id: 34
canceled: false
order: 259
worker_model: codex:sol
worker_effort: xhigh
worker_sizing_rationale: Inherited provider, model, and effort from Relay Runner General Settings.
worker_provider_notes: Use my defaults preserves explicit stable provider selections; Codex resolves Sol/Terra/Luna then uses model_reasoning_effort and Claude uses --effort.
---

## Description

Fix the remaining installed-app regression where the first Backlog card does
not reliably receive hover and click events in the Workspace.

RR-254 (`05faba8`) is an ancestor of the currently packaged `main`, and its
stable scroll-document hit-routing code is present in the installed binary.
Its focused synthetic tests passed, but the user has now repeated the missing
hover/click behavior in the real mounted application. Treat that as evidence
that the accepted tests still omit part of the actual window-to-panel-to-scroll
document event path.

Trace the genuine mounted pointer route and make the smallest correction.
Preserve scroll, drag, drop-strip, card identity, selective interception, and
non-draggable card behavior. Do not claim success from direct containment or
isolated `hitTest` calls if the actual mounted event is still routed elsewhere.

## Acceptance criteria

- [x] A regression test fails on current `main` using the mounted Workspace hierarchy and the same event path used by a real mouse or trackpad.
- [x] Entering the first Backlog card immediately applies its hover state and expected cursor without requiring a click, scroll, tab switch, or window refocus.
- [x] Clicking the first Backlog card selects and opens that exact ticket once.
- [x] Top, center, and bottom regions of the first card resolve to the first card after initial mount and after scrolling.
- [x] Second and later cards retain correct hover, click, and drag identity; drop strips and non-draggable cards remain distinct.
- [x] Snapshot replacement, prepend/reorder/height changes, scope changes, tab transitions, and window activation do not leave stale pointer ownership.
- [x] Coordinate conversion is verified across screen, window, content, clip, document, and flipped-view spaces where they participate in the mounted route.
- [x] Focused `BoardOverlayScrollViewTests` and `ProgramBoardStatusTests` pass without deleting RR-254 identity-specific lifecycle coverage.

## Post-merge smoke

- [ ] After the reviewed branch is merged and one combined app is installed,
  physically verify first-card hover and click. Worker attempts 1–3 could not
  perform this check because the refreshed ad-hoc app lost macOS Screen
  Recording and Accessibility authorization. This is an explicit human
  follow-up, not a passed worker acceptance check.

## Evidence

- RR-254 implementation commit `2ba10ed` and merge commit `05faba8` are present
  on current `main`.
- The installed app binary matches the packaged artifact, so this is not an
  installation mismatch.

## Run log

- **Run 27** (attempt 1) — branch `relay/rr-259` — partial
  - Enabled mouse-move delivery on the nonactivating Workspace panel and first-click acceptance on card event views; the shared UI path is provider-neutral for Codex and Claude.
  - Added a full mounted panel/reveal/SwiftUI/scroll-document regression covering screen-to-document coordinates, first-card hover/cursor, and exact single-click selection.
  - Verified 25 `BoardOverlayScrollViewTests`, 63 `ProgramBoardStatusTests`, the RR-237 board benchmark, and the canonical DMG build; installed and packaged binaries hash-match and codesign verification passes.
  - Follow-up: the packaged real-pointer smoke is blocked because the refreshed ad-hoc app no longer has Screen Recording/Accessibility authorization. Re-grant those permissions to Relay Runner, relaunch it, then verify physical hover and click on the first Backlog card.

- **Run 28** (attempt 2) — branch `relay/rr-259` — partial
  - Replaced the test's direct card method calls with real `NSApplication` → `BoardOverlayPanel.sendEvent` dispatch; it failed before the fix and now covers first-card hover/click, second-card hover ownership, and drag capture outside the card.
  - Selectively routed mounted card pointer events at the panel boundary while leaving scroll, drop strips, and non-card controls on AppKit's normal path; the shared Workspace behavior is provider-neutral for Codex and Claude.
  - Verified 25 `BoardOverlayScrollViewTests`, 63 `ProgramBoardStatusTests`, the opt-in RR-237 board benchmark, the canonical DMG build, deep code signing, and identical installed/packaged binary hash `e94982f5cf5577f97e9d7e2361e71d85690ec051ae15efdce85784155e232f76`.
  - Follow-up: partial because the packaged real-pointer smoke is still blocked by Relay Runner's explicit Accessibility denial, and the running app process predates the refreshed installed binary. Grant Accessibility to Relay Runner, relaunch it, then verify physical hover and single-click opening on the first Backlog card.

- **Run 30** (attempt 3) — branch `relay/rr-259` — partial
  - Relaunched Relay Runner onto the refreshed installed binary, verified packaged/installed SHA-256 `e94982f5cf5577f97e9d7e2361e71d85690ec051ae15efdce85784155e232f76`, valid deep code signing, and mounted the Workspace through RelayActions.
  - Re-ran 25 `BoardOverlayScrollViewTests` and 63 `ProgramBoardStatusTests`; all 88 passed. No additional code change was justified because the attempt-2 implementation remains green and the remaining criterion requires packaged real-pointer evidence.
  - Partial: after relaunch RelayVision reports Screen Recording is not granted, and macOS still presents its Accessibility authorization warning. Grant both permissions to Relay Runner, relaunch, then manually verify first-card hover and exact single-click opening; the shared Workspace path is provider-neutral for Codex and Claude.

- **Run 31** (attempt 4) — branch `relay/rr-259` — done
  - Re-reviewed the mounted `NSApplication` → `BoardOverlayPanel.sendEvent` implementation from runs 27–28; it selectively routes hover, click, and drag capture to the exact card while preserving the provider-neutral Codex/Claude Workspace path, so no further code change was justified.
  - Re-ran 25 `BoardOverlayScrollViewTests` and 63 `ProgramBoardStatusTests`; all 88 passed, including the mounted first-card event-path regression.
  - Done for worker review: synced the latest `main` clarification that physical verification belongs to the combined post-merge app. RelayVision still reports Screen Recording denied and RelayActions still sees macOS's Accessibility warning, so that human smoke check remains explicitly unresolved.

- **Run 34** (attempt 6) — branch `relay/rr-259` — done
  - Reapplied the already-reviewed pointer implementation commits `ed1ea5d` and `e2549cb` onto current `main` without redesigning the fix, rebuilding, or reinstalling the app.
  - Recovered and combined the prior run logs while preserving current `main`'s unchecked `## Post-merge smoke` requirement.
  - Verified 25 `BoardOverlayScrollViewTests` and 63 `ProgramBoardStatusTests`; all 88 passed. Source/test acceptance is complete, and physical first-card hover/click verification remains the explicit post-merge human follow-up.
