---
id: RR-292
title: Restore the Add project Workspace action
status: verification_blocked
priority: high
depends_on: [RR-282]
run_id: null
verification_blocker: The split-menu source, transition sequencing, focused tests, rebuilt installed bundle, signature, and daemon health pass. Mounted inspection of the first build exposed SwiftUI's borderless menu injecting a leading disclosure mark; the corrected button-menu build is installed, but after its TCC-driven relaunch Relay Vision reports No displays detected while Window Server's display shield/loginwindow is above the app, so it cannot capture the final presentation and picker animations.
verification_resume: In installed PID 73592, once the secure display shield is no longer active, confirm Add project has one rounded split outline with the text on the left and a divider plus dedicated chevron segment on the right. Choose and cancel both menu actions; confirm Workspace completes its dismiss animation before each panel opens and uses its reveal animation when each panel closes.
canceled: false
order: 292
execution_mode: implementation
worker_model: balanced
worker_effort: medium
worker_sizing_rationale: The change is a bounded SwiftUI wiring repair with focused route and presentation tests.
worker_provider_notes: The project-management interaction is app-owned and must present equivalent project scope to Codex and Claude sessions.
---

## Description

Restore the registry-v2 Projects header action specified by RR-273. The control must be labeled **Add project** and cover both supported project-registration paths: adding an existing Git repository and creating a new project folder. Reuse the existing project-management handlers so their folder panels, validation, Workspace suspension/restoration, and cancel semantics remain authoritative.

Also prevent the registry-v2 Workspace from rendering legacy registry projects. A project card shown in registry-v2 must be backed by the same registry used to confirm Start Session scope.

The empty-registry state must show only its heading and the two project actions, without a repeated subtitle or manual Refresh action. The populated header action must use the native dropdown interaction with a visible disclosure chevron and a split-button face: a text segment, a dedicated disclosure segment, and a divider inside one rounded outline. Open and Save panels must activate Relay Runner only after Workspace completes its standard dismiss animation, and Workspace must use its standard reveal animation when either panel closes.

## Acceptance criteria

- [x] In registry-v2, the Projects header shows **Add project** instead of **Select all**.
- [x] The action offers both **Add existing project** and **Create new project** and routes to the existing supported flows.
- [x] Existing-project selection still validates a Git repository, and successful add/create returns with the new project selected.
- [x] Cancel leaves project and Workspace state unchanged; the Workspace is restored after either external panel closes.
- [x] Registry-v2 Workspace snapshots never display legacy-only project cards.
- [x] The bounded compatibility-off path retains its legacy behavior without weakening the registry-v2 scope gate.
- [x] The empty-registry state has no subtitle or Refresh action and retains both project actions.
- [x] The populated **Add project** control is a native dropdown button with a visible disclosure chevron and no manually presented AppKit menu.
- [x] Existing-project and create-project panels activate Relay Runner only after Workspace has been suspended, so the external panel can become frontmost automatically.
- [x] The populated **Add project** dropdown matches the approved split-button treatment with separate text and chevron regions inside one rounded outline.
- [x] The native menu does not inject a second leading disclosure mark; the only chevron is in the right-hand segment.
- [x] Choosing either project action completes the normal Workspace dismiss animation before its system panel opens.
- [x] Closing either system panel restores Workspace through the normal reveal animation.
- [x] Focused Swift tests pass, and the rebuilt installed app is ready for mounted interaction verification.
- [ ] The mounted empty and populated project states match the revised presentation, and both external panels surface frontmost and restore Workspace unchanged after cancel.

## Verification plan

Run the focused header-presentation and Workspace activity snapshot tests, build the supported app bundle, validate the installed bundle and daemon, then verify the mounted **Add project** menu and both choices.

## Inline implementation log

- Implemented inline at the user's direction; no worker run was created.
- Replaced the registry-v2 header's Select all control with an Add project menu that routes to the existing Add Existing and Create handlers. The compatibility-off path retains its legacy Select all action.
- Corrected Workspace activity scope resolution so registry-v2 cannot render legacy-only project cards that Start Session cannot confirm.
- The focused three-test regression set passed. The expanded ProgramBoardStatus, ProjectRegistryV2, and WorkspaceActivitySnapshot suites passed 82 tests with one intentional benchmark skip and zero failures; `git diff --check` passed.
- `scripts/build-dmg.sh` rebuilt and installed the ad-hoc RR-292 app. Staged and installed executable SHA-256 values match at `854ab8fa99010f4358c8ca41c1413ae116ac0ee1e83f919d999761bd7ad7c067`; both bundles pass strict deep code-sign verification.
- Relaunched installed PID 53270 with the explicit login-session `RELAY_RUNNER_REGISTRY_V2=1` test gate. The daemon health endpoint and installed `orchestrator`/`followup_tickets` imports pass. Relay Vision could not capture mounted evidence because Relay Runner lacks Screen Recording permission, so user-visible menu verification remains blocked rather than inferred.
- Follow-up mounted feedback removed the repeated empty-state subtitle and Refresh action, replaced the SwiftUI Menu label with the shared Settings-style action button, and made both Open and Save project panels activate Relay Runner after Workspace suspension.
- The follow-up focused and adjacent suites passed 203 tests with one intentional performance skip and zero failures. `git diff --check` passed.
- Rebuilt, installed, and relaunched the follow-up as installed PID 56172 under `RELAY_RUNNER_REGISTRY_V2=1`. Staged and installed executable SHA-256 values match at `479473488b21302eca1adaed7381e9e9488b375b9ea8e0aa288ffcdbba756a19`; the installed bundle passes strict deep code-sign verification, installed service imports pass, and `/v1/health` returned `{"ok": true, "version": "RelayOrchestrator/0.1"}`. Mounted interaction remains verification-blocked pending the user's active test.
- Mounted feedback then confirmed the generic Settings-style action did not surface either project choice. The user chose to restore the earlier native dropdown interaction rather than keep the custom AppKit popup presenter.
- Restored the native SwiftUI `Menu` with a visible disclosure chevron and both existing project handlers, retained shared control height and padding, and removed the custom AppKit `NSMenu` presenter entirely.
- The focused board suite passed 64 tests. The adjacent overlay, registry-v2, Workspace activity, and directory-picker suites passed 34 tests with one intentional performance skip; all 98 tests completed with zero failures and `git diff --check` passed.
- Rebuilt and installed the app with `scripts/build-dmg.sh`. Staged and installed executable SHA-256 values match at `a26287edbe2a68cebc5e1979c17dc86c3042ffbe4716376004bd67a159e6f12d`; the installed bundle passes strict deep code-sign verification, PID 66072 is running with `RELAY_RUNNER_REGISTRY_V2=1`, and `/v1/health` returned `{"ok": true, "version": "RelayOrchestrator/0.1"}`.
- Relay Vision's mounted check was attempted but ScreenCaptureKit returned a TCC denial despite the permission status reporting granted. The user-visible click remains the only verification blocker.
- Follow-up mounted feedback requested the approved split-button face and the standard Workspace transitions around both project panels. The native `Menu` remains authoritative; only its label layout changed to distinct text and disclosure regions inside one rounded outline.
- Added an animated external-window suspension path used only by project picker flows. The existing Workspace dismiss sequence finishes before the Open or Save panel starts, and the cached Workspace container is reset and revealed through the standard animation after the panel closes.
- The focused suites passed 84 tests. The expanded Workspace transition, registry, permission, notch, ActionGlow, and hosted-tool suites passed 239 tests with one intentional performance skip and zero failures; `git diff --check` passed.
- Rebuilt and installed the follow-up app. Staged and installed executable SHA-256 values match at `384b752ca3c42bb17a6c7f5770c67d90f64d284aa606fa8f1e2c8cfeba449837`; both bundles pass strict deep code-sign verification, installed services import under the supported Homebrew Python, PID 71578 is running with `RELAY_RUNNER_REGISTRY_V2=1`, and `/v1/health` returned `{"ok": true, "version": "RelayOrchestrator/0.1"}`.
- Relay Vision remains denied Screen Recording, so the revised mounted appearance and picker animation checks remain with the user's active test rather than being inferred from source.
- After the user enabled Screen Recording, mounted inspection showed that `.borderlessButton` was still injecting a leading disclosure mark despite the custom label. Replaced that native style with `.button` plus a plain button face so the custom right-hand split label remains authoritative without changing the native `Menu` interaction.
- The corrected focused board suite passed 64 tests with zero failures. Rebuilt and installed executable SHA-256 values match at `c1d1b095c6154676941ab80dd3573300eafeee2dc4572d9907d623335bf7a1f7`; both bundles pass strict deep code-sign verification, installed services import, PID 73331 is running with `RELAY_RUNNER_REGISTRY_V2=1`, and daemon health passes.
- The corrected ad-hoc install reset macOS Screen Recording again, so final mounted button and picker-animation confirmation remains verification-blocked with the user rather than inferred from source.
- Relay Runner subsequently relaunched as PID 73592. Two final Relay Vision attempts reached ScreenCaptureKit but returned `No displays detected`; the custom Relay window inventory showed Window Server's `Display 1 Shield` and loginwindow above the still-open Workspace, so the secure-display boundary is preserved as the remaining blocker.
