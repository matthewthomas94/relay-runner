---
id: RR-256
title: Keep background playback gestures and test previews isolated
status: done
priority: high
depends_on: []
run_id: null
canceled: false
order: 256
worker_model: balanced
worker_effort: high
worker_sizing_rationale: This focused reliability fix crosses AppKit global event-monitor lifecycle, the first-play control path, and Python test isolation from a live overlay socket.
worker_provider_notes: Codex and Claude share the same Relay Runner gesture and speech stack, so both providers receive the same fix and verification.
---

## Description

Make double-tap Option playback reliable while Relay Runner is in the
background, including after an event monitor initially fails to attach during
launch or permission recovery. The app currently retains its local NSEvent
monitor but silently drops a nil global monitor, leaving playback dependent on
whether Relay Runner happens to be frontmost.

Keep voice-service tests isolated from a running Relay Runner instance. A
worker running the voice bridge unit suite published its fixture text
`fresh response` to the real `/tmp/voice_state.sock`, replacing the live
response pill with an unplayable test preview. Tests must never publish state
to the live overlay unless an explicit integration harness opts in.

## Acceptance criteria

- [x] A failed global Option/Control/Shift event-monitor installation is visible in diagnostics and is retried without duplicating local or global monitors.
- [x] When a later retry succeeds, background double-tap Option events continue through the existing `__PLAY__` path without requiring Relay Runner to be frontmost.
- [x] Monitor teardown cancels pending retries and removes every installed monitor exactly once.
- [x] Focused Swift coverage deterministically exercises initial global-monitor failure followed by successful recovery.
- [x] Voice bridge unit tests stub or redirect waiting-preview publication by default and cannot write fixture text to a live Relay Runner state socket.
- [x] Focused Python coverage preserves authoritative preview behavior through explicit injected publishers.
- [x] Existing first-play, replay, stale-response, confirmation, and Workspace double-tap behavior remains unchanged for Codex and Claude sessions.

## Run log

- 2026-07-29: Inline diagnosis reproduced two missed playback gestures while the embedded Codex session and TTS bridge remained healthy. No `__PLAY__` reached the bridge. A concurrent RR-255 test run then leaked the fixture `fresh response` into the live overlay, identifying a separate test-isolation defect in the same user-visible playback path.
- 2026-07-29: Added retrying, diagnostic global modifier-monitor installation with deduplicated local/global monitors and retry cancellation during teardown. The recovered monitor feeds the existing provider-neutral `__PLAY__` path.
- 2026-07-29: Isolated voice bridge unit tests from the live waiting-preview socket by default while retaining explicit publisher injection for authoritative preview coverage.
- 2026-07-30: Verified merged `main` with all 381 Python tests and 65 focused Swift gesture, command-delivery, state-machine, text-only pill, mounted-scroll, and onboarding tests passing.
- 2026-07-30: Built the canonical release artifacts with `scripts/build-dmg.sh`, refreshed `/Applications/Relay Runner.app`, verified the installed bundle signature, matched the installed and distribution binary SHA-256 hashes, and relaunched version 0.4.27 (build 31) as a new process.
