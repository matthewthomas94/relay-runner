---
id: RR-294
title: Keep Relay Vision ActionGlow topmost and out of captures
status: done
priority: high
depends_on: []
run_id: null
verification_blocker: null
verification_resume: null
canceled: false
order: 294
execution_mode: implementation
worker_model: balanced
worker_effort: medium
worker_sizing_rationale: The fix is bounded but crosses AppKit window ordering and ScreenCaptureKit content filtering.
worker_provider_notes: Relay Vision and ActionGlow are app-owned surfaces and must behave identically for Codex and Claude sessions.
---

## Description

Keep the Relay Vision ActionGlow perimeter visual above every Relay Runner app surface, including Workspace and the status notch. Treat the visual as user feedback rather than screenshot content: Relay Vision captures must explicitly exclude ActionGlow windows, and the panels must also opt out of general macOS window sharing where supported.

## Acceptance criteria

- [x] ActionGlow panels use a window level above Workspace and the status notch.
- [x] Later Workspace or notch ordering cannot place those surfaces over an active ActionGlow.
- [x] Relay Vision ScreenCaptureKit filters explicitly exclude Relay Runner's ActionGlow panels.
- [x] ActionGlow panels opt out of general window sharing/capture.
- [x] Focused window-policy and Relay Vision tests pass.
- [x] A rebuilt installed app is ready for mounted topmost and capture-exclusion confirmation.
- [x] A successful mounted Relay Vision screenshot keeps ActionGlow visible to the user while omitting it from captured pixels.

## Verification plan

Add focused policy coverage for ActionGlow identification and window ordering, run the adjacent Workspace and hosted-tool suites, rebuild and install, then use the custom Relay Vision path for mounted confirmation when Screen Recording is available.

## Inline implementation log

- Implemented inline during the user's active installed-app test loop; no worker run was created.
- Raised ActionGlow one window level above the status notch and two levels above Workspace. The installed custom Relay window inventory lists `Relay Runner ActionGlow` first, ahead of the notch and both full-screen Relay Runner surfaces while Workspace is open.
- Marked every ActionGlow panel as non-shareable and gave it a stable Relay-owned identity. Relay Vision's ScreenCaptureKit content filter also excludes matching windows from the current process before capture.
- The focused suites passed 84 tests. The expanded adjacent suite passed 239 tests with one intentional performance skip and zero failures; `git diff --check` passed.
- Rebuilt and installed PID 71578. Its staged and installed executable SHA-256 values match at `384b752ca3c42bb17a6c7f5770c67d90f64d284aa606fa8f1e2c8cfeba449837`, strict deep code-sign verification passes, and the daemon health endpoint returned `{"ok": true, "version": "RelayOrchestrator/0.1"}`.
- A mounted Relay Vision screenshot was attempted but macOS reported that Screen Recording is not granted. Capture-exclusion UAT remains verification-blocked rather than inferred from the passing policy checks.
- After the user enabled Screen Recording, Relay Vision captured successfully. A stronger mounted check then fired a successful Relay Actions command and immediately captured within ActionGlow's ten-second active window; the glow remained visible to the user but no perimeter visual appeared in the returned 3024x1964 image.
- A later menu-style-only rebuild changed the app's ad-hoc signature and reset Screen Recording again. That rebuild did not change the already proven ActionGlow window or ScreenCaptureKit exclusion paths; RR-294 is complete on the mounted evidence above.
