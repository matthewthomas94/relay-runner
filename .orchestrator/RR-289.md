---
id: RR-289
title: Gate rollout with installed two-device verification
status: done
priority: high
execution_mode: implementation
depends_on: [RR-288]
run_id: null
canceled: false
order: 289
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: The final gate spans disposable remotes, permissions, migration, failure injection, fresh install, mounted UI, provider parity, telemetry, and staged default changes.
worker_provider_notes: Codex and Claude must pass equivalent installed project-scope, artifact, sync, lifecycle, and recovery scenarios; any provider-specific launch/auth evidence must be recorded separately without weakening the shared gate.
---

## Description

Implement RR-273 delivery phase 9 and run RR-270's complete automated and manual verification strategy against a signed installed build. Add or complete the disposable two-device remote harness, failure injection, permission/access recovery, retention/archive/restore, dirty-source isolation, provider-parity, migration/rollback, fresh-install/reset recovery, and mounted Workspace UI coverage.

Rollout remains evidence-gated: first an explicit per-project opt-in, then default enablement for newly registered projects only, then an offered legacy migration after safety evidence and operational recovery documentation are accepted. The source implementation and verification tooling are complete here. By explicit user direction on 2026-08-05, executing the signed installed two-device matrix is deferred to backlog ticket RR-290; this deferral records no accepted installed evidence and enables no later cohort.

## Acceptance criteria

- [x] Unit/integration suites cover the complete RR-270 fixture matrix, including exact age boundaries, exemptions, archive/restore, dependencies, sync conflicts, source isolation, allowlist/privacy, registry recovery, migration, and provider parity.
- [x] A disposable two-device remote harness proves unrelated offline edits rebase/push normally, same-ticket/config/attachment conflicts stop for explicit resolution, and no force/source publication path exists.
- [x] The signed installed-app matrix for empty Workspace, Add/Create/Remove, grants/revocation, moved/offline recovery, scope, history/restore, conflict UI, fresh install/reset, and mounted interaction is preserved without a passing claim in backlog RR-290.
- [x] Installed dirty/staged/untracked/local-ahead isolation evidence is preserved without a passing claim in backlog RR-290; source and disposable harness coverage remains recorded separately.
- [x] Installed Codex/Claude parity evidence is preserved without a passing claim in backlog RR-290, including the documented intentional launch/auth/model/effort differences.
- [x] Failure injection covers registry corruption, writer/CAS/materialization crashes, daemon restart, auth/offline/push races, missing objects, interrupted migration, review/merge failure, and rollback without data loss or recursive retry.
- [x] Per-project opt-in defaults off and has a documented rollback; new-project default remains gated on accepted opt-in evidence; legacy migration remains an explicit offer gated on accepted new-project evidence.
- [x] Telemetry/diagnostics are bounded and privacy-safe, expose rollout cohort and recoverable failure states, and never include secrets, raw audio/transcripts, raw logs, or hidden reasoning.
- [x] Release documentation records final ownership/state machines, operator recovery, storage/privacy limits, the current rejected ad-hoc artifact identity, and the exact signed evidence deferred to RR-290.
- [x] RR-273 is reconciled after all implementation children land; the user-authorized deferral closes RR-289 without enabling later cohorts or representing installed/physical evidence as accepted.

## Rollback boundary

Every rollout cohort has an independent kill switch that stops new artifact writes/sync only after draining or safely freezing active work. Rollback preserves repositories, artifact refs/history, registry backups, and migration journals and never silently downgrades or discards canonical data.

## Verification plan

Run source suites first, then package and sign the supported build, execute disposable two-device and failure-injection harnesses, perform fresh-install/reset recovery, and capture installed/mounted UI and provider-parity evidence. Source-only success cannot satisfy the installed gate.

## Inline implementation log

- Implemented inline; no RR-289 worker run was created. The rollout control plane, independent cohort kill switches, bounded diagnostics/evidence, disposable remote harness, installed gate, and packaged command-line tools are complete on the current branch.
- Hardened the rollout boundary after integration audit: the daemon now checks configured opt-ins on every lifecycle lookup, explicit policy opt-out wins, paused or recovered policy state fails closed without legacy-writer fallback, and the signed gate requires an actual Developer ID Application authority rather than only a Team ID and non-ad-hoc signature. The focused 20-test set passed.
- Checkout source harness passed all seven executable scenarios and nine source-matrix groups (`ff8fa880c2eb73e11a3b3104998f85427027e488ce38a132f4c93b2449c195cd`). Full Python passed 549 tests on the clean final run; full Swift passed 668 tests with 3 intentional benchmark skips and 0 failures. The provider-native 150,000-token automatic-compaction test passed in the Swift suite.
- The latest bundle built and installed. Its packaged source harness passed (`c3433d2a6a3007367b2120b903794e3182ffaae8aae2a8cb0392386e8946fa5e`), but `inspect-installed` correctly returned `signed_installed_build_required`: version `0.4.29`, build `33`, ad-hoc signature, no Team ID, and zero valid local signing identities.
- Exact passing evidence, exclusions, blocker, and future verification steps are in `docs/verification/RR-289-artifact-rollout.md`. Both later cohorts remain disabled; RR-290 retains the deferred work in backlog.

## Scope decision

On 2026-08-05 the user explicitly deferred the signed installed two-device verification for later. RR-289 therefore closes the completed implementation and source/package verification work. RR-290 owns every deferred physical criterion. No evidence outcome was changed, and `new_project_default` plus `legacy_migration_offer` remain disabled.
