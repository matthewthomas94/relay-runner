---
id: RR-307
title: Repair ready-sweeper Git repository fixtures
status: backlog
priority: high
execution_mode: implementation
depends_on: []
run_id: null
canceled: true
order: 307
worker_model: balanced
worker_effort: medium
worker_sizing_rationale: The failure is isolated to one test helper, but the repair must preserve RR-299's production safety check and prove the full Python suite.
worker_provider_notes: none
---

## Description

Repair the ready-sweeper test fixtures exposed by RR-299's recoverable dirty-source integration change. `tests/test_ready_sweeper.py` currently represents repositories by creating ordinary `.git` directories. The production `_source_checkout_blocker` now correctly executes `git status`; those fake repositories cannot answer that check, so eight otherwise unrelated sweeper assertions receive `integration_blocked` instead of exercising their intended dispatch, active-run, awaiting-merge, dependency, and program-scope behavior.

Make the smallest fixture-only correction so tests that require a repository use a real temporary Git repository. Do not weaken, bypass, or globally mock the production cleanliness check. Preserve the production behavior for genuine non-repositories, unavailable Git status, clean repositories, dirty repositories, active runs, and reviewed runs awaiting merge. Avoid source changes unless the fixture-only approach is proven insufficient.

This repair unblocks RR-306's release preflight. Do not change release versions, merge the previous RR-306 branch, push, tag, publish, install, or relaunch Relay Runner.

## Acceptance criteria

- [ ] Ready-sweeper fixtures that exercise Git cleanliness use valid temporary Git repositories rather than directories merely named `.git`.
- [ ] All 18 `tests.test_ready_sweeper` tests pass, including dispatch, active-run, awaiting-merge, dependency-promotion, project-scope, and repo-scoped identity cases.
- [ ] The production `_source_checkout_blocker` behavior and its nonfailure `integration_blocked` lifecycle remain unchanged; the repair does not mock or bypass the check under test.
- [ ] The full Python service suite passes after the fixture repair, and `git diff --check` passes.
- [ ] The implementation changes only `tests/test_ready_sweeper.py` and RR-307 unless a directly necessary additional test file is justified in the run log.
- [ ] No release version, tag, publication, installed app, private demo content, or unrelated ticket is modified.

## Run log

- 2026-08-09: PM reproduced run 22's release blocker on canonical `main`: `python3 -m unittest tests.test_ready_sweeper` ran 18 tests with 8 failures. Every failure was routed to `integration_blocked` because `ReadySweeperTests.make_repo` creates a fake `.git` directory that cannot satisfy the new real `git status` preflight.
- 2026-08-09: Canceled before dispatch because active RR-306 run 23 implemented the exact fixture-only repair and passed all 18 focused ready-sweeper tests. RR-306 remains the canonical release-preparation run.
