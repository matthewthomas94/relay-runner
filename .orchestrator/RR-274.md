---
id: RR-274
title: Diagnose and permanently restore double-Option replay
status: verification_blocked
priority: high
depends_on: []
run_id: 7
verification_blocker: Relay Runner lacks Screen Recording permission, RelayActions cannot emit a modifier-only Option gesture, and no physical HID route is connected, so foreground/background double-Option replay cannot be observed through started and played.
verification_resume: Grant Relay Runner Screen Recording and Accessibility and provide a physical or modifier-only HID Option route, then explicitly resume RR-274 for foreground/background replay verification.
canceled: false
order: 274
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: A recurring installed-app regression crosses global modifier monitoring, Swift gesture polling, FIFO controls, speech ownership, replay eligibility, TTS process lifecycle, and mounted verification.
worker_provider_notes: Codex and Claude share the gesture and speech stack, but their completion and lifecycle events must both leave the same replayable final state; cover both explicitly.
---

## Description

Diagnose and fix the recurring production regression where double-tapping Option no longer replays the most recent eligible Relay Runner speech. The failure has persisted across several attempts, so do not treat this as another isolated metadata toggle or assume that passing unit tests prove the installed behavior.

Establish the failing boundary end to end before changing code: global and local `NSEvent` delivery; `CapsLockGesture` double-tap recognition; `STTEngine` polling and timestamped `__PLAY__`; bridge routing through `play_or_replay`; speech-coordinator replay selection and freshness; `TTSWorker` retained audio/text and process lifecycle; and actual audio start/completion. Distinguish a missing gesture from a dropped FIFO control, an ineligible or superseded speech intent, a lost replay buffer, and an audio-process failure. Also distinguish first play from replay after a confirmed initial `played` event, and deferred Queue playback from Auto mode.

Treat RR-252, RR-256, RR-271, and the recent `1e44b81` progress-speech replayability fix as prior evidence, not proof of correctness. Explain which prior assumption or test boundary allowed the current regression to survive. Preserve a privacy-safe diagnosis in this ticket's run log, then implement the smallest durable repair at the authoritative ownership boundary rather than adding another independent timeout or fallback.

## Acceptance criteria

- [x] Reproduce the current failure on the supported installed Relay Runner build and in a deterministic test harness before implementing the repair; record which end-to-end boundary drops or rejects replay without logging response or transcript content.
- [x] The diagnosis traces one correlated double-Option attempt through modifier detection, gesture polling, FIFO receipt, replay selection, TTS preparation, audio-process start, and terminal speech lifecycle state, clearly distinguishing an absent stage from a delayed one.
- [ ] After an eligible utterance has reached `played`, one valid double-tap Option replays that utterance audibly exactly once without requiring a second gesture, whether Relay Runner is frontmost or in the background.
- [x] Replay works for the latest eligible authoritative final, work-lifecycle utterance, and command-scoped progress utterance, including the progress path changed by `1e44b81`; non-replayable handoffs or suppressed speech are handled according to the documented policy.
- [x] Deferred Queue mode (`auto_play = false`) retains the current response for first play and subsequent replay, while Auto mode retains a completed utterance for replay; switching modes does not silently discard the authoritative replay target.
- [x] Replay ownership survives normal TTS process completion and cleanup, but never resurrects canceled, failed, stale, superseded, secret, or unrelated test-fixture speech.
- [x] Rapid duplicate Option gestures cannot double-start or overlap speech. Double-tap Control cancellation, confirmation yes/no handling, onboarding practice, double-tap Shift, ordinary Option-modified typing, and newest-command freshness remain correct.
- [x] Global modifier-monitor installation, failure, recovery, and active monitor identity are observable with privacy-safe diagnostics, and retry does not install duplicate local or global monitors.
- [x] Focused Swift tests exercise real modifier down/up timing, foreground/background monitor paths, retry recovery, confirmation arbitration, and exactly one emitted play control per accepted gesture.
- [x] Focused Python tests exercise the actual `__PLAY__`/`play_or_replay` boundary, first-play-versus-replay state, Queue and Auto modes, completed TTS process cleanup, progress/final/lifecycle eligibility, cancellation, supersession, and Codex/Claude completion paths.
- [ ] The final verification observes `started` and `played` for the replayed utterance; `REPLY_ACCEPTED`, intent acceptance, enqueue, waiting-preview publication, or `queued` alone do not count as audible success.
- [ ] A freshly built installed app passes a mounted smoke test with repeated physical double-Option replay attempts in both foreground and background. If environment permissions prevent this validation, keep that item open and report the exact permission blocker rather than claiming completion.
- [x] The run log records the root cause, why RR-252/RR-256/RR-271/`1e44b81` coverage missed it, the minimal repair boundary, tests run, installed build identity, and the exact absence of mounted replay evidence.

## Run log

- **Run 6** (attempt 1) — branch `relay/rr-274`
  - Traced four privacy-safe installed-app attempts on 0.4.29 (build 33): modifier detection, timestamped `__PLAY__`, FIFO receipt, and visual acknowledgement were present, but replay selection emitted neither `replayed` nor `started`. The deterministic root defect was the coordinator's single completed-target slot: a newly stale utterance could mask an earlier still-eligible work result. RR-252 covered one current target, RR-256 stopped at gesture/FIFO recovery, RR-271 covered retained-play latency, and `1e44b81` covered progress metadata; none exercised selection behind a stale most-recent completion.
  - Replaced the slot with a bounded coordinator-owned replay history that selects the newest fresh completed target and logs privacy-safe rejection reasons. Added active monitor identities/recovery diagnostics, exactly-once foreground/background down-up coverage, confirmation arbitration, and suppression of false playback from Option-modified typing. The shared bridge/coordinator path preserves Codex/Claude parity.
  - Verified 427/427 Python tests and 52/52 focused Swift gesture/FIFO/state tests; the full Swift suite ran 621 tests with the nine pre-existing provider-model/launch expectation failures and three skips. **Partial:** the installed services matched source before the repair, but a post-repair fresh install and physical foreground/background smoke were blocked by `loginwindow`; RelayActions also cannot emit a modifier-only tap. No post-repair mounted `started`/`played` evidence is claimed.
- **Run 7** (attempt 2) — branch `relay/rr-274`
  - Preserved repair commit `de22838`, then passed 138 focused Python coordinator/TTS/bridge tests and 11 Swift modifier/FIFO tests. The canonical DMG build from branch HEAD `44a91d7` installed `com.relayrunner.app` 0.4.29 (build 33); the deep ad-hoc signature passed and staged/installed executable, `speech_coordinator.py`, and RelayActions hashes matched.
  - Launched repaired app PID 78142 and restarted only `com.relay.voicebridge` to repaired Python PID 78494 while foreground Codex PID 62411 stayed alive. In Queue mode, a privacy-safe command-scoped authoritative final was queued, first-played through the coordinator, and correlated through `tts_preparing`, `started`, `afplay_started`, and terminal `played`, establishing a fresh retained post-fix replay target.
  - **Partial:** before the physical replay, RelayActions again reported missing Accessibility and RelayVision reported missing Screen Recording for the rebuilt app, and the display returned to `loginwindow`. `pmset -g assertions` showed both app PIDs holding `PreventUserIdleSystemSleep` while `PreventUserIdleDisplaySleep` remained 0, so the enabled setting kept work alive but did not keep the display unlocked. No connected physical HID command route was available, no post-fix `option_detected` event was observed, and foreground/background physical double-Option replay remains open; unlock the Mac (and regrant Relay Runner Accessibility/Screen Recording for tool-assisted observation) before retrying.
- **Lifecycle reconciliation** — recovery commit `faf3e86` and branch commit `25d7877` preserve the same stable patch id (`519e0333`) as `de22838`. The implementation and automated evidence are reviewable, but no physical replay event exists, so RR-274 is intentionally `verification_blocked` and RR-279 remains blocked.
