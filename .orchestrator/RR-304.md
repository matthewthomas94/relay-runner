---
id: RR-304
title: Make recording and playback dot matrices audio-reactive
status: done
priority: high
execution_mode: implementation
depends_on: []
run_id: 4
canceled: false
order: 304
worker_model: strong
worker_effort: high
worker_sizing_rationale: The visual change crosses real-time microphone and playback signal extraction, smoothing, audio-thread safety, AppKit rendering, lifecycle cleanup, accessibility, and mounted audio verification.
worker_provider_notes: Recording occurs before provider delivery and playback uses the shared TTS path, so Codex and Claude must expose identical audio-reactive overlay behavior.
---

## Description

Make the existing recording and playback dot-matrix glyphs respond to live audio energy so their motion reads like a restrained pseudo waveform. During recording, animate from the user's microphone signal. During response playback and replay, animate from the speech audio being played. Silence should settle toward a calm baseline and stronger speech should produce visibly greater movement without becoming noisy or visually unrelated to the audio.

Preserve the current visual design. Keep the existing 24-point artwork bounds, twelve-dot arrangement, dot sizes, white/accent color assignments, orange Listening treatment, blue Playing treatment, notch placement, and surrounding overlay geometry. Change only the motion behavior needed to make the dots audio-reactive; do not redesign the glyph, add a conventional waveform, change the notch layout, or alter Working and Not working artwork.

Derive a normalized, privacy-preserving level envelope from the in-process recording and playback pipelines. Downsample and smooth it before UI delivery so audio callbacks remain real-time safe and the main thread is not flooded. The visual mapping must be deterministic, bounded, and resilient to silence, clipping, device changes, cancellation, playback failure, replay, and rapid recording/playback transitions. Do not persist raw samples or level history and do not send audio telemetry outside the existing audio path.

## Acceptance criteria

- [x] Recording dot motion responds to the live microphone amplitude envelope, with a calm near-silence state and visibly stronger but bounded motion during speech.
- [x] Initial playback and explicit replay respond to the amplitude envelope of the speech audio actually being rendered, rather than using an unrelated looping animation.
- [x] The existing twelve-dot geometry, 24-point bounds, dot diameters, white/accent assignments, orange Listening palette, blue Playing palette, notch placement, and surrounding overlay design remain unchanged.
- [x] The pseudo-waveform mapping changes dot position and/or opacity only within documented safe bounds, preserves legibility at peaks, and never clips outside the existing artwork footprint.
- [x] Audio level extraction is allocation-safe and nonblocking on real-time callbacks; UI updates are rate-limited, delivered safely to the main actor, and smoothed with documented attack/release behavior so speech feels responsive without jitter.
- [x] Silence, clipping, unavailable levels, device switches, capture restart, playback preparation, stop, cancel, failure, replay completion, and state interruption reset or settle the animation without stale motion leaking into another state.
- [x] Reduce Motion uses a static or materially reduced-motion Listening/Playing presentation while retaining the current status colors and clear state distinction.
- [x] Working and Not working motion, foreground/background playback ownership, media pause/resume, recording delivery, TTS audio, replay eligibility, and cancellation behavior are unchanged.
- [x] No raw audio samples, per-frame level history, transcripts, or derived voice biometrics are persisted, logged, or transmitted for the animation.
- [x] Deterministic tests cover silence, representative speech envelopes, peaks/clamping, smoothing, reset behavior, Reduce Motion, recording/playback state changes, and unchanged glyph geometry/colors.
- [ ] Focused audio and notch tests pass, and a fresh built app completes mounted microphone-speech plus TTS-playback/replay checks showing visible audio correlation without capture glitches, playback underruns, or layout changes.
- [ ] The run log records the signal source for recording and playback, update frequency, smoothing and bounds, measured audio/UI impact, automated checks, and the mounted verification performed.

## Run log

- **Run 4** (attempt 1) — branch `relay/rr-304`
  - Added a latest-value, try-lock microphone RMS meter to the 16 kHz STT sample path and a pacing thread that reads the exact PCM16 WAV consumed by `afplay` for both initial playback and replay. Only normalized current levels cross the existing local state socket; no samples or level history are retained, logged, or sent elsewhere. Codex and Claude use these same capture and TTS paths.
  - Replaced Listening/Playing loop motion with a deterministic four-column pseudo-wave mapping capped at 1.25 points inside the unchanged 24-point/twelve-dot artwork. Levels update at 20 Hz, normalize from -55 to -12 dBFS, smooth with 70 ms attack and 240 ms release, reset through capture/playback lifecycle changes, and become static under Reduce Motion. Microbenchmarks measured 13.33 microseconds per 8,000-sample microphone buffer and 0.67 ms per 50 ms playback frame on this Mac.
  - Implementation is complete and automated verification passed: 760 Swift tests (3 benchmark skips), 33 TTS replay tests, 28 speech-coordinator tests, release app/DMG/zip packaging, deep code-sign verification, and source-to-bundle service hashes. The packaging script also refreshed `/Applications/Relay Runner.app`; it was not launched. Physical microphone plus initial TTS/replay visual correlation remains unperformed, so the ticket is verification blocked until the named mounted check is completed.
- **Risk-accepted closure (2026-08-14)** — After successful review and integration, the user explicitly authorized Done and deferred the fresh mounted microphone plus initial TTS/replay visual-correlation check to the next build. That mounted verification remains unperformed and is not recorded as passing.
