---
id: RR-103
title: Add ticket-level worker sizing metadata
status: done
priority: high
depends_on: [RR-102]
run_id: 286
canceled: false
---

## Description

Add a permanent orchestrator check that every dispatchable ticket has an explicit worker-sizing decision before a sub-agent runs.

The foreground orchestrator should decide the expected worker effort as accurately as possible while it still has the full project overview. A cold worker should not have to infer whether it should run on a fast/cheap model, a balanced model, or a high-effort configuration.

Define the smallest durable metadata shape for ready tickets, likely in frontmatter, to record:

- Worker model or model tier.
- Worker reasoning effort where the provider supports it.
- A short sizing rationale that explains why the ticket needs that level.
- Any provider-specific limitation, especially if Claude cannot map the same effort fields directly.

Likely touch points:

- `docs/specs/orchestrator-tickets.md`
- `services/instructions/orchestration-workflow.md`
- `services/orchestrator_workflow.md`
- `Sources/relay-orchestrator-mcp/Instructions.generated.swift`
- `scripts/relay-orchestrator`
- Installed skill templates generated by `scripts/relay-orchestrator`

## Acceptance criteria

- [x] Ready-ticket guidance requires an explicit worker-sizing decision before dispatch.
- [x] Ticket schema docs define the metadata fields, allowed values, and whether they are required for `ready` tickets.
- [x] Orchestrator-facing instructions tell the foreground session to choose model/effort based on ambiguity, code blast radius, security risk, test/build cost, and expected implementation depth.
- [x] Worker-facing instructions explain that the assigned sizing came from the orchestrator and should not be silently weakened.
- [x] Generated MCP/instruction artifacts are regenerated or verified so Codex and Claude receive the same permanent policy.
- [x] Provider parity is explicit: Codex reasoning effort and Claude model/tier behavior are both addressed, with intentional limitations documented.

## Run log

- **Run 286** (attempt 1) — branch `relay/rr-103`
  - Added ready-ticket sizing fields and allowed values to the ticket schema docs, including Codex `model_reasoning_effort` and Claude `--effort` parity from RR-102.
  - Updated foreground orchestration guidance, worker prompt guidance, installer skill templates, and regenerated `CLAUDE.md` plus relay-orchestrator MCP instructions.
  - Verified `python3 scripts/build-instructions --check`, `bash -n scripts/relay-orchestrator`, `git diff --check`, and `swift build --product relay-orchestrator-mcp`; RR-104 remains the planned dispatch-enforcement follow-up. Status: done.
