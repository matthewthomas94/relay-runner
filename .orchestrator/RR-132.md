---
id: RR-132
title: Route worker completion through orchestrator review
status: done
priority: high
depends_on: [RR-130]
run_id: 200
canceled: false
order: 132
worker_model: strong
worker_effort: high
worker_sizing_rationale: "Review and merge automation is high-risk because it controls branch integration, retries, done state, and dependency progression."
worker_provider_notes: "Preserve provider parity for Codex and Claude worker branches, logs, failure modes, and merge/retry behavior."
---

## Description

Change worker completion handling so completed worker tickets go to the persistent orchestrator for technical review, retry/fix decisions, merge, and done-state publication.

The current flow leaves integration to the foreground session or human after a worker succeeds. RR-127 moves technical acceptance to the orchestrator. When a worker completes, the orchestrator should inspect the branch/run evidence, decide whether to accept, request fixes/retries, or split follow-up work, then merge accepted work and move tickets to `done`.

The PM should keep fetching and summarizing status, but should not be the technical reviewer.

## Acceptance criteria

- [x] Worker run success triggers or exposes a review-needed event for the persistent orchestrator.
- [x] The orchestrator can inspect worker run logs, ticket run logs, branch diffs, and verification evidence.
- [x] The orchestrator can request a fix/retry or additional tickets when work is incomplete.
- [x] When accepted, the orchestrator merges the worker branch, resolves expected ticket-state updates, prunes the worker worktree/branch, and publishes the ticket as `done`.
- [x] Dependency progression continues to work after orchestrator-owned merge/done publication.
- [x] PM status fetching reports review, retry, merge, done, and failure states clearly.
- [x] Add tests/harness coverage for accept, reject/retry, merge conflict handling, done-state publication, and dependency auto-progression.

## Run log

- **Run 200** (attempt 1) - branch `relay/rr-132`
  - Changed successful worker completion to enter `AwaitingReview`, exposed review evidence plus accept/retry operations over HTTP and MCP, and kept retry/merge behavior provider-neutral for Codex and Claude runs.
  - Accept now merges the worker branch into the source repo, verifies the merged ticket is `done`, prunes the worktree/branch, and only then progresses dependents; retry explicitly marks the reviewed run failed, prunes, and can dispatch a fresh attempt.
  - Added Python harness coverage for review evidence, accept/merge, merge conflict handling, retry, done-state publication, dependency progression, and PM review/conflict reporting; ran full Python unittest discovery and `swift test`.
  - Unresolved follow-ups: none. Ticket done.
