---
id: RR-69
title: Build latest Relay Runner DMG for Downloads
status: done
priority: high
depends_on: []
run_id: 57
canceled: false
---

## Description

Build the current Relay Runner app from the local `main` checkout and place the resulting DMG in `~/Downloads` for the user.

Use the existing packaging script:

```bash
./scripts/build-dmg.sh
```

The script supports unsigned local builds when `SIGN_IDENTITY` and `NOTARY_PROFILE` are unset. Do not make source changes unless the build fails for a concrete repo issue; if that happens, keep the fix minimal and explain it in the run log.

After the build completes, copy the newest generated `.dmg` from `dist/` to `~/Downloads/`.

## Acceptance criteria

- [x] `./scripts/build-dmg.sh` completes successfully from the repo root.
- [x] The generated Relay Runner `.dmg` is copied to `~/Downloads/`.
- [x] The final run log includes the copied DMG path and a size or timestamp check proving it exists.
- [x] Any build failure is reported with the failing command and the relevant error excerpt.

## Run log

### Run 57 (attempt 1) — branch `relay/rr-69`

- Ran `./scripts/build-dmg.sh` from the repo root successfully with `SIGN_IDENTITY` and `NOTARY_PROFILE` unset; generated `dist/RelayRunner.dmg`.
- Copied the newest DMG to `/Users/matthewthomas/Downloads/RelayRunner.dmg`; verified size `6581623` bytes, mtime `2026-06-12 17:18:42 AEST`.
- No source changes were needed. Ticket done.

Unresolved questions or follow-ups: none.
