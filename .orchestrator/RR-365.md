---
id: RR-365
artifact_id: ticket-1164edbaeada8c790134010031b82cb71d4d8315
user_edited_at: 2026-09-20T15:46:44.850100Z
title: Notes: capture meeting audio and transcribe continuously on device
status: ready
priority: high
execution_mode: implementation
depends_on: [RR-364]
run_id: null
canceled: false
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: Continuous audio capture and ASR require careful timing, bounded resources and isolation from the existing command pipeline.
worker_provider_notes: Codex and Claude use the same project note contracts and authorization rules; capture starts neither provider. Normal authorized work sessions may read saved notes as project context.
run_state: retry_requested
claimed_at: 2026-09-20T15:29:35.769670Z
activity_at: 2026-09-20T15:52:01.635110Z
run_outcome_at: 2026-09-20T15:52:01.635110Z
merge_outcome_at: 2026-09-20T15:18:55.936540Z
---

## Reopened capture-ownership correction — run 147 follow-up

Run 147 was accepted and merged as 18cd91fb86440722fd47044d61176d90afcf42a3 (worker ab6d0fafedbfcf85c3449c267efa04fef9712fde), but independent deterministic PM validation proves that the current rapid-restart ownership requirement still fails. Reopen this same authorized ticket for that bounded correction; preserve the integrated implementation and prior passing fixes. The merged run remains a truthful historical record, not proof that this ticket is complete. RR-368/RR-367/RR-372 must remain dependency-gated until the correction is reviewed and merged.

Start from current main. Do not replay or cherry-pick the already merged implementation stack. Read the current canonical ticket, especially the start-attempt ownership clarification and the rapid-restart probe below. Its three unchanged production sources now match worker commit ab6d0fafedbfcf85c3449c267efa04fef9712fde and the merge exactly. Use /tmp/rr365-run147-rapid-restart-probe/candidate/Probe.swift, result.txt, source-sha256.json and the parent README.md as reproducible evidence, not just counters.

The current unconditional failed-start stop fixes the old leak but leaves both late success and late failure cleanup able to stop a newer successful adapter. The obsolete start's zero-success cleanup can also close the newer ingress. Across typed failure, ordinary failure and successful completion of the delayed old attempt, the fixture observes live owner [2] becoming [], retained audio [22] instead of [22, 33], source state still capturing and zero reported drops. Old callback sample 91 is correctly excluded. Fix the complete attempt lifetime with the smallest coherent ownership/serialization change: every old attempt eventually stops, old callbacks and cleanup cannot mutate a newer recording, and new accepted content remains intact. If serialization makes this exact overlap unreachable, adapt the deterministic gates to prove that guarantee rather than treating a timeout as a pass.

Add meaningful delayed-success/failure and rapid-restart regressions with public-state, resource-ownership and actual retained-content assertions. Preserve existing pause/stop tail, overload, replay, common timing, typed-format, source-recovery and old-system-stream tests. The worker and independent reviewer must explicitly assess this recorded failure before acceptance, not infer coverage from the existing 31 focused tests or the previous full suite (913 tests, 3 skipped, 0 failures). No installed/hardware validation is claimed. Do not alter unrelated tickets, providers, features or the accepted non-dispatchable Backlog document UI.

## PM review evidence — late start after ingress overload

A separate deterministic synthetic probe on the unfinished run 148 candidate at 2026-09-20T15:42:48Z found a remaining capture-resource leak. This is candidate evidence to assess on the final source, not a claim about an untested later commit or installed behavior. Reproduction, unchanged production-source copies, compile inputs and SHA-256 manifest are in /tmp/rr365-run148-overload-start-probe/ (Probe.swift, source-sha256.json, result.json, result.txt). The sampled MeetingNoteCaptureSession.swift hash is 43ae98fb910e939404102f552158d72b9039f4915bd9a65ef66fce113b2661df.

The adapter emits two startup frames with maximumPendingFrames=1, forcing either ingress overflow or bounded startup-buffer overflow before its explicit setup gate is opened. The probe waits for the overload path to stop the adapter, then opens that gate so the old start acquires resources and returns successfully. Observed result: before completion stopCount=1/live=false; after old completion stopCount=1/live=true; after final session.stop() stopCount=1/live=true. The start operation throws because ingress has closed, yet its late resource remains live. No sleep selects the interleaving; the setup gate opens only after observed teardown. The process exits 1.

In this candidate, startSources' stale-success guard changed from unconditional capture.stop() to stopIfStarted(capture). consume's overflow/error cleanup can remove startedCaptures before start returns; the later conditional stop therefore skips newly acquired resources. Serialization of pause/stop alone does not cover this failure path. Preserve the complete start-attempt cleanup invariant across overload and other ingress failure as well as pause/stop, while retaining the new rapid-restart content/ownership guarantees. Assess this same fixture against the final source (or explain and test an intentional lifecycle change); keep source fixture, native review and installed evidence distinct. Do not bypass the native review lifecycle or interrupt a healthy active worker/reviewer.

## Description

Add a continuous local meeting transcription path inside the existing macOS app, reusing suitable FluidAudio/Parakeet components while separating its destination and lifecycle from STTEngine voice commands. The recorder produces document content and runs without starting Codex, Claude, the voice bridge, the messenger or a title/summary model.

Retain microphone plus computer/meeting audio in the implementation scope. Establish a documented source matrix against Relay Runner's current supported OS/hardware and select the smallest capture implementation supported by the evidence in RR-359. Do not silently ship a mic-only interpretation, raise the app OS floor, or promise every device combination. Resolve ordinary API/engine choices in implementation; report a concrete limitation or necessary product tradeoff if the supported baseline cannot meet the scope.

Use ordered segment identities and audio time ranges to reconcile changing partial hypotheses and final text. The current TranscriptAccumulator removes text found anywhere in earlier text; that behavior alone is not proof of meeting fidelity. Genuine repetitions and a later repeated sentence must survive. Use local STT readiness/error handling and existing model terms; no new foundation-model, title-generation or summary prerequisite.

Foreground work sessions and note-taking are mutually exclusive. Start Note Taker ends the existing foreground work session through its established End Session lifecycle, waits for bridge/audio/TTS/UI ownership to be released, and then begins local note capture. Start Session from either recording or paused note mode stops capture, drains the accepted final tail and durably saves the same note to its original project before launching the requested work session. These explicit start actions authorize the switch without a second confirmation. A save or teardown failure blocks the destination mode and preserves recovery state; remote sync alone does not block a locally saved switch. Unrelated daemon worker runs are unaffected.

One authoritative transition owner serializes mode changes and rejects stale callbacks. Snapshot the destination project when the start action is accepted while retaining the source note's original project. Repeat clicks, project changes, start failures and delayed teardown must never leave both foreground modes active or misfile a note.

Within note mode, actual Caps Lock ON pauses capture and OFF resumes the same note. Paused speech from every source is excluded from both retained audio and later transcription; preserve already accepted content and the pre-pause tail. Pause does not finalize, allocate a new ID or launch an agent. Read the actual key state at startup, so Caps Lock already ON starts paused before accepting audio. Route the key exclusively to the active mode; paused notes still own note mode. Synchronize rapid/repeated changes and restore ordinary work-mode key behavior after teardown.

## Ownership and scope

Relay Runner repository. Own capture adapters, local model readiness and the continuous transcript producer. Emit typed source/timing/segment revisions under RR-364 contracts. RR-368 owns durable orchestration and replay; RR-367 owns user controls.

## Required inputs

None beyond the declared ticket dependencies. The in-app product direction, Git-backed transcript storage, note ID format and ordinary agent access are already decided.

## Acceptance criteria

- [ ] A fixture-driven meeting recording starts, transcribes and stops with no agent/bridge process, provider API call, voice-command FIFO write, gesture-triggered dispatch or transcript logging. Existing Start Session voice behavior continues to work.
- [ ] Microphone and supported computer/meeting audio are both captured, including simultaneous local and remote speech. Document source selection, permissions, channel/mixing strategy and the initial tested device/OS matrix; unavailable computer audio is visible and never silently presented as whole-meeting capture.
- [ ] Ordered segment IDs, source IDs, audio-time ranges and revision/finality markers distinguish overlapping ASR windows from actual repeated speech. Tests cover repeated phrases/sentences, silence, partial replacement, finalization, overlap, delayed/out-of-order results and multiple capture epochs without duplicated or suppressed content.
- [ ] Capture and ASR buffers/queues remain bounded during a reproducible long-meeting fixture. Measure backlog, memory, processing latency and dropped-frame/segment counts on the stated hardware; backpressure and failures are surfaced instead of silently discarding speech.
- [ ] Stop drains the accepted audio tail and produces an explicit final boundary for RR-368. Missing/corrupt models, denied/revoked permissions, microphone disconnects, route/sample-rate changes and sleep/wake produce recoverable typed state and preserve already emitted content.
- [ ] Publish a resumable producer/checkpoint contract for RR-368 with timing epochs and enough information to replay unfinished local audio. Document any unavoidable gaps and prevent a restarted producer from repeating already committed segments.
- [ ] Local transcription remains usable offline once its required model is available. Existing model acquisition/readiness UX is explicit, with no hosted transcription fallback or automatic title/summary generation. Record dependency/model license and attribution decisions.
- [ ] Use synthetic or appropriately consented fixtures for engineering validation. Distinguish fixture results from actual installed audio-path evidence, which RR-372 owns; do not mark hardware support proven by unit tests.
- [ ] Capture obeys the RR-368 exclusive mode owner. Work-to-note teardown releases the existing voice/bridge/TTS/audio path before meeting capture starts; note-to-work drains accepted audio and exposes a final boundary before the coordinator can start a work session. No background worker cancellation is introduced.
- [ ] Caps Lock ON gates all note audio sources off at the pause boundary, preserving accepted pre-pause audio for finalization but retaining no paused speech for later replay. OFF resumes the same producer/note with a new timing epoch as needed. Startup with Caps Lock ON accepts no meeting audio until OFF; rapid toggles do not duplicate or leak paused content.

## Implementation anchors

Sources/relay-runner/STT/STTEngine.swift, TranscriptAccumulator.swift and CapsLockGesture.swift. STTEngine currently writes voice FIFO/status events; TranscriptAccumulator is in-memory and suppresses contained/overlapping text; CapsLockGesture.isCapsLockOn() reads the actual system flag.

## Scope revision — 2026-09-20

Combines surviving capture scope from RR-365, local ASR/model readiness from RR-366 and focused capture/engine evaluation from RR-362. The standalone-app feasibility gate is removed.

The user-directed current scope is recorded at the top of RR-359. Its earlier pinned research remains evidence, while its separate-app/private-store recommendation is historical. This is an authorized implementation ticket in the scoped first-cut queue; only actual completed validation may be claimed. No implementation was performed during this revision.

## Execution authorization — 2026-09-20

The user authorized implementation, review, integration and appropriate installed verification of only the revised first-cut feature on 2026-09-20. The execution allowlist is RR-364, RR-365, RR-367, RR-368, RR-370 and RR-372. Use the declared dependencies and existing worker/reviewer workflow; do not promote or execute unrelated backlog or canceled/historical tickets. All revised canonical scopes and cancellations were verified before promotion. Required external verification inputs remain genuine gates, never assumed passes.

## PM review clarification — 2026-09-20

The full-transcript requirement still applies to run 139's bounded-ingress correction. Verify retained audio and content, not only queue size or counters: a synthetic, uniquely identifiable burst within capacity must survive a deliberately slow accepted-audio sink and both pause/stop drains without omission, duplication or reordering. Exercise both sources, retained checkpoint bytes/descriptors and the resulting final content where appropriate. Beyond capacity, preserve accepted content under the existing typed recovery/error contract; overload must never report a successful complete final boundary after audio loss.

Caps Lock ON is the pause boundary. Close or gate all source ingress before any asynchronous adapter teardown can retain speech captured after the pause request, while draining the already accepted pre-boundary tail. Test a delayed adapter stop with distinguishable pre-boundary and post-boundary samples; only the former may reach the sink, checkpoint, transcript or later replay. Treat stale callbacks and resume epochs consistently.

At the 11:47 UTC inspection of the in-progress run 139 diff, the new overload test used a sink that recorded only a count, and the pause test accepted samples emitted while adapter stop ran. These are specific review concerns, not a final verdict on an unfinished worker. Inspect the final committed correction and its tests. Keep the existing worker/reviewer lifecycle; this clarification authorizes no duplicate worker, new ticket, review bypass or unrelated changes.

### Connected capture transitions — run 142

Assess the connected capture lifecycle interleavings together within the existing RR-365 scope. Use deterministic synthetic ordering controls and actual retained-content/public-state assertions, beyond counters and queue-size checks:

- Cover an adapter start returning both before and after a failure event is processed. Failed or unavailable source state must remain visible until a legitimate recovery; late startup success or stale samples must not present a failed source as capturing or the recording as complete whole-meeting capture.
- Cover start completion after pause and after stop, callbacks concurrent with ingress closure, and delayed shutdown. Every started or still-starting adapter must ultimately be stopped, with no stale callback reviving capture or the next epoch.
- With a slow accepted-audio sink, drain uniquely identifiable queued pre-boundary content from both sources in order while excluding every post-boundary sample from retained audio, checkpoints, final text and later replay. Preserve the same note identity across pause/resume.
- Preserve the existing overload and recovery/error contract: retained accepted content survives; loss or unavailable source conditions stay explicit and cannot be reported as successful complete capture. Retain the prior verified shared monotonic timing and checkpoint/replay regressions.

This is a coherent check of already-required guarantees, not new scope. Continue the existing worker and independent reviewer lifecycle; do not create another ticket or duplicate worker, change models, interrupt healthy workers or bypass review. The feature's current board layout remains note/document cards in Backlog, with no Notes column.

### Fresh successful restart content — run 143 PM probe

A separate synthetic PM review probe found a connected transition gap in run 143's new per-source gate; native independent review must assess it before acceptance. This is source/fixture evidence only, not installed or physical audio proof. After a microphone source fails, pause closes its old ingress. On resume, its fresh `capture.start()` emits sample 22 before returning successfully; the old `blockedSources` membership silently discards that new-generation sample. Once start returns, the source is shown as capturing and finalization reports zero dropped samples. The prior accepted sample 11 remains, but expected retained microphone content [11, 22] is observed as [11].

The probe uses two synthetic sources, a controlled start-return gate, and a later healthy-source sample 77 as an ordered ingress barrier to prove the fresh microphone frame was consumed before the successful start return. It does not depend on a sleep to order the missing frame. Preserve failed/interrupted-source exclusion and old-ingress rejection, while ensuring legitimate frames produced by a fresh successful restart are retained or buffered through the start boundary; they must not silently vanish under a successful final result. Add a content/state regression covering failure -> pause -> fresh successful start, alongside explicit recovery and the existing timing/replay cases.

Reproduction and exact copied-source SHA-256 manifest: `/tmp/rr365-run143-restart-probe/Probe.swift`, `result.txt`, and `source-sha256.json`. The three production sources were copied unchanged from run 143's working tree based on 820e2b14f73ea78822ffd3c1038783d1a295c8eb plus the recorded per-source gate diff. `MeetingNoteCaptureSession.swift` SHA-256 b57ed1f119ac5108d72803f4acee6907a94aa2da597bf8e01a96a0ea70e5128a. Standalone fixture exited 1 with `microphone_retained=[11.0]`, state `capturing`, successful final boundary and `dropped_samples=0`; this finding is not a claim that native review has already rejected the run.

### Start-attempt ownership across rapid restart — run 147

Review the complete resource lifetime of each capture start attempt, including success and throwing completion after pause/stop teardown. Run 146 reproduced an adapter that acquired resources and threw after ownership had been removed, leaving it live. The correction must stop every old attempt while ensuring that old cleanup and failure callbacks cannot stop or corrupt a newer successful recording after rapid pause/resume. An unconditional stop must not trade the old resource leak for stopping the new generation.

Use deterministic delayed-success, delayed-failure and rapid-restart interleavings, with retained-content and adapter-ownership assertions. Verify old attempts are ultimately inactive, the new successful attempt remains active and retains its own distinguishable audio, and no old/post-pause content reaches saved audio, checkpoints, final transcript or later replay. Preserve truthful source state and the existing passing replay, shared timing, typed-format and old-system-stream isolation fixes. Keep the implementation as small as these existing exclusive-capture guarantees allow.

This is clarification of accepted scope, not a new feature. Continue the existing run 147 worker and native reviewer lifecycle without duplicate dispatch, interruption or review bypass. Notes remain non-dispatchable Backlog documents with no Notes column. These are synthetic/source checks; installed, physical audio and external validation remain separate RR-372 obligations.

### Rapid-restart ownership probe — run 147 baseline

A deterministic synthetic PM probe reproduced the rapid-restart concern against the restored run-146 source at 72e70c7307f154fe9ee1a7c13eadabadfa1d7695, before run 147's correction. Hold attempt 1 before resource acquisition; complete pause, resume attempt 2 successfully and retain its sample 22; then release the old attempt to acquire resources and either return successfully, throw a typed failure or throw an ordinary error. All three cases stop the newer attempt: live resource owners change from [2] to [], later sample 33 is lost, retained content is [22] rather than [22, 33], while public source state still says capturing and the final boundary reports zero dropped samples. Old callback sample 91 is correctly excluded.

The unchanged production session also lets the obsolete start's zero-success cleanup close the current ingress. Review both per-adapter ownership and session/ingress cleanup, not only the immediate catch-path stop. The regression must either safely serialize the old attempt through cleanup before restarting, or isolate each attempt so delayed old completion cannot affect the new recording. Do not claim the unfinished run 147 correction has already failed: rerun or adapt the regression against the final source, preserving deterministic ordering and actual retained-content/resource-owner assertions.

Reproduction: /tmp/rr365-run147-rapid-restart-probe/Probe.swift, result.txt and source-sha256.json. The fixture copies three production Swift files unchanged; MeetingNoteCaptureSession.swift SHA-256 is db72c2c36f436571ef13767cd3460218e0eba0846eb1c1b91e635d40b5b23126. Only synthetic adapters and transcription are used; this is no installed or physical capture proof.

## Run log

- **Run 136** — retry requested: Review requested retry: RR-365 needs follow-up before merge. The checkpoint/replay path is not lossless or idempotent: a later successful final window removes every pending descriptor through its end, including audio from an earlier failed transcription; replay ignores nextWindowSequenceByEpoch, clears buffered samples when the epoch changes, and starts from a retained chunk's raw start, so boundary-crossing chunks can re-emit committed audio and pending audio from earlier epochs can be lost. Add regression tests for failed-window-then-success, a checkpoint with a chunk crossing an owned-window boundary, and multiple pending epochs. Also establish a shared monotonic/PTS-based note timeline for microphone and system audio; current per-source accepted-sample counters timestamp a delayed system-audio start at 0 and cannot correctly align simultaneous local/remote speech.
- **Run 138** — retry requested: Review requested retry: Review found a blocking capture-ingress and teardown gap. MeetingNoteCaptureSession creates an untracked Task for every audio frame, so a slow accepted-audio sink can build an unbounded task/mailbox backlog, and pause/stop has no barrier to drain frames already emitted by an adapter before the producer changes state. Also, handle(.failed) removes a source from activeSources while stopSources stops only active sources, so a running ScreenCaptureKit stream that reports an unsupported format can be skipped during teardown. Use a bounded, tracked ingress/drain path, always stop every started adapter, and add regressions for burst input with a slow sink, pre-boundary tail draining, and post-start capture failure teardown.
- **Run 139** — retry requested: Review requested retry: RR-365 still violates the clarified Caps Lock pause boundary. MeetingNoteCaptureSession.stopSourcesAndDrainIngress awaits stopStartedCaptures() before captureIngress.finish(), so a delayed adapter can emit new samples after pause is requested and those samples reach the accepted-audio sink, checkpoint, transcript, and replay. The new test testPauseDrainsFrameEmittedWhileAdapterStops explicitly expects those teardown-time samples to survive, which is the opposite of the ticket's PM review clarification. Close or generation-gate ingress at the pause/stop request first, drain only items already accepted before that boundary, still stop every started adapter, and replace the regression with distinguishable pre-boundary queued samples plus post-boundary teardown samples (covering both sources as appropriate) that proves only the pre-boundary tail survives. Update the capture spec to match.
- **Run 140** — retry requested: Review requested retry: Review found two blocking capture-boundary races. MeetingCaptureIngress.finish() marks finished under its lock but unlocks before continuation.finish(), while submit() never checks finished, so a callback can enqueue post-pause audio in that interval and have it drained as pre-boundary content; add an atomic close/submit gate and a concurrent boundary regression. Also, stopStartedCaptures() removes adapters that may still be inside start(); if stop runs before that start installs its live stream, the post-start guard calls stopIfStarted(), sees ownership already removed, and leaves the newly started adapter running. Preserve in-flight ownership or unconditionally stop after a stale start, with a delayed-start teardown regression. Update the capture spec's stale 19-test count to the observed 20.
- **Run 141** — retry requested: Review requested retry: Review found a blocking source-state race in MeetingNoteCaptureSession.startSources: an adapter can emit and fully process .failed before capture.start() returns (for example, an immediate ScreenCaptureKit unsupported-format callback), setting the source unavailable, but the successful return then unconditionally calls markSourceCapturing and overwrites the final public state to capturing. A deterministic delayed-return regression reproduced final state capturing after the failure, which can falsely present computer audio as active/whole-meeting capture. Track startup event generation/state so a failure processed during start cannot be overwritten, stop or otherwise retain correct ownership for the failed adapter, and add a regression where .failed is consumed before start returns and the final state remains unavailable.
- **Run 142** — retry requested: Review requested retry: Review found a blocking failed-source gating race. MeetingNoteCaptureSession processes every frame through producer.ingest even after the same source emitted .interrupted or .failed, and ingest unconditionally publishes that source as capturing. AudioCaptureLifecycle keeps the microphone backend live during its delayed route rebuild, while a failed system-audio adapter also remains live until teardown, so a stale post-event callback can persist audio, create the next epoch, and overwrite interrupted/unavailable before any explicit .recovered event. This violates run 142's requirement that failed state remain visible until legitimate recovery. Gate frames per source after interrupted/failed until an explicit recovery or fresh successful start, and add deterministic session regressions proving post-failure/post-interruption frames cannot revive capture or enter retained audio/checkpoints/transcript.
- **Run 143** — retry requested: Review requested retry: RR-365 still drops legitimate audio on failure -> pause -> fresh successful restart. MeetingNoteCaptureSession leaves the source in blockedSources while awaiting capture.start(); because the actor is reentrant, a frame emitted by the new start before it returns is consumed by the guard and silently discarded. The deterministic final-source probe retained microphone [11] instead of [11, 22], yet reported the source capturing, emitted a successful final boundary, and reported zero dropped samples. Buffer or generation-tag fresh-start frames until start succeeds (while still rejecting stale old-ingress callbacks), and add the ticket-required failure -> pause -> fresh successful start content/state regression. Focused MeetingTranscriptProducerTests remain 25/25 green, so the current suite does not cover this interleaving.
- **Run 144** — retry requested: Review requested retry: Review requested retry: RR-365 does not preserve typed audio-format failures. MeetingSystemAudioCapture emits MeetingAudioCaptureFailure.unsupportedFormat, and microphone conversion/format recovery errors are also available upstream, but MeetingNoteCaptureSession funnels every non-permission failure through sourceBecameUnavailable; MeetingTranscriptProducer therefore emits source_unavailable and MeetingCaptureIssueCode.formatChanged is never used. This violates the acceptance requirement that route/sample-rate/format failures produce recoverable typed state, and leaves RR-367/RR-368 unable to distinguish format changes from ordinary source loss. Preserve the format category through both startup and runtime failure paths, emit format_changed, and add deterministic regressions for system-audio unsupported format and microphone format/conversion recovery.
- **Run 145** — retry requested: Review requested retry: Review found a blocking stale ScreenCaptureKit callback race. MeetingSystemAudioCapture stores one mutable sample/event handler and both SCStreamOutput/SCStreamDelegate callbacks ignore the stream instance they receive. After pause/stop and a fresh start, an output or didStopWithError callback queued from the old SCStream can read the new handlers, be tagged with the new capture generation, and either retain stale/post-boundary audio or mark the newly restarted source unavailable; the session generation gate cannot reject it because it arrives through the new closure. Bind callback delivery and start/stop cleanup to the active SCStream identity or an adapter generation, and add a deterministic regression covering old-stream audio and failure callbacks after restart. The 28 focused tests and full 910-test suite pass, so the current suite does not cover this adapter-level interleaving.
- **Run 146** — retry requested: Review requested retry: Review found a blocking late-start teardown race. If pause/stop removes and stops an adapter while capture.start() is still in flight, and that start later acquires resources then throws, both catch paths call stopIfStarted(); ownership is already absent, so stopIfStarted returns without stopping the now-live adapter. A deterministic regression using the existing delayed fake plus startFailure reproduced stopCount 1 and isRunning true after pause; the committed late-start test covers only successful return. Unconditionally stop a late-completing throwing adapter after teardown (or preserve in-flight ownership), and add failure-after-pause/stop coverage that proves every adapter is stopped and cannot emit stale callbacks.
- **Run 147** — reviewed source merge 18cd91fb86440722fd47044d61176d90afcf42a3: Added continuous local dual-source meeting capture and transcription with bounded ingress, resumable checkpoints, typed recovery state, pause and stop gating, stale-callback protection, and unconditional cleanup for adapters whose late startup acquires resources and then fails. Verification: swift test --filter MeetingTranscriptProducerTests: 31 tests passed with 0 failures; synthetic 60-minute dual-source fixture accepted 7,200 chunks with max queue depth 2, max buffer 4,720 bytes, and zero drops.; swift test: 913 tests executed, 3 skipped benchmarks, 0 failures.; Deterministic pause and stop regressions verify late failed starts are stopped again and cannot remain running.; git diff --check source_start_head..HEAD passed; immutable artifact snapshot hashes remained unchanged; no lifecycle paths were committed.
- **Run 148** — retry requested: Review requested retry: RR-365 still leaks a late-started capture after ingress overload. In consume(), stopStartedCaptures() removes the in-flight adapter from startedCaptures; if capture.start() then acquires resources and returns successfully, startSources() reaches its stale-success guard but stopIfStarted() skips capture.stop() because ownership was already removed. I recompiled /tmp/rr365-run148-overload-start-probe against the exact final worker source SHA-256 43ae98fb910e939404102f552158d72b9039f4915bd9a65ef66fce113b2661df; it exited 1 with stopCount=1/live=true after old completion and still live=true after final session.stop(). Preserve complete start-attempt cleanup across overload/consume teardown and add a deterministic regression for late resource acquisition after ingress closure. The new RapidRestart tests pass 3/3 but do not exercise this path.
