---
id: RR-68
title: Validate OTA update lifecycle with Relay services
status: done
priority: medium
depends_on: [RR-66, RR-67]
run_id: 56
canceled: false
---

## Description

Add validation and product handling for the update lifecycle around Relay Runner's bundled services.

Relay Runner is not just a menu-bar binary. The installed app ships helper MCP binaries, shell scripts, and Python services. Existing work in RR-60 handles stale orchestrator schema recovery after app/service updates; OTA updates must preserve that behavior and make the user experience deliberate when a new app bundle lands.

This ticket should verify the complete lifecycle from an older installed app to a newer installed app using Sparkle artifacts, especially with active or recently active Codex/Claude sessions.

Implementation guidance:

- Confirm Sparkle replacement preserves the bundle id, signing identity, and TCC attribution for Relay Actions/Relay Vision helpers.
- Ensure active voice/orchestrator services do not keep running stale code indefinitely after the app updates.
- Reuse or extend the existing stale-daemon restart logic rather than creating a second service-management path.
- If an update cannot install while a session is active, surface a clear user action instead of silently failing.
- Add a manual verification document if the full signed/notarized Sparkle flow cannot be exercised in unit tests.

## Acceptance criteria

- [x] A documented old-version to new-version OTA test proves the app updates and relaunches from `/Applications`.
- [x] After update/relaunch, bundled MCP binaries and Python services resolve from the new app bundle.
- [x] Existing orchestrator stale-schema recovery still works after OTA update.
- [x] Active Codex and Claude session behavior is explicitly considered; any intentional restart/deferral behavior is documented.
- [x] TCC-sensitive helpers keep attribution to `Relay Runner` after update.
- [x] Failure modes such as app running from DMG, missing appcast, unsigned archive, or active-session deferral produce actionable UI/log output.
- [x] Verification includes `swift test`, Python tests where relevant, and packaging/appcast validation commands.

## Run log

### Run 56 (attempt 1) — branch `relay/rr-68`

- Added Sparkle relaunch handling to stop bundled voice services, restricted Sparkle startup to the installed `/Applications/Relay Runner.app`, and reused `relay-orchestrator --restart-if-idle` for installed orchestrator launch-agent refresh after OTA relaunch.
- Added `docs/verification/RR-68-ota-update-lifecycle.md` and linked it from release docs; it covers old-to-new OTA checks, new-bundle service resolution, Codex/Claude active-session parity, TCC helper attribution, stale-schema recovery, and failure modes.
- Verified with `swift test --skip-update`, `python3 -m unittest discover -s tests -p 'test_*.py'`, `plutil -lint Info.plist`, `bash -n` for packaging/appcast scripts, workflow YAML parse, `python3 scripts/build-instructions --check`, and `git diff --check`. No unresolved code follow-ups; full signed/notarized Sparkle OTA remains a manual release-artifact check captured in the new doc. Status: done.
