---
id: RR-258
title: Keep embedded providers alive after Start Session readiness
status: done
priority: urgent
depends_on: []
run_id: 38
canceled: false
order: 258
worker_model: codex:sol
worker_effort: xhigh
worker_sizing_rationale: Inherited provider, model, and effort from Relay Runner General Settings.
worker_provider_notes: Use my defaults preserves explicit stable provider selections; Codex resolves Sol/Terra/Luna then uses model_reasoning_effort and Claude uses --effort.
---

## Description

Fix the installed Start Session path so an embedded Codex or Claude process
remains interactive until the user, provider, or app explicitly ends it.

The 2026-07-30 installed build was verified to match packaged `main` at
`4ece979` and to contain RR-255. Despite that, diagnostic attempt
`8015a2a9-d39a-405b-9aba-1f85aa49cb63` recorded bridge socket readiness,
provider spawn, and interactive readiness at `09:37:10Z`, followed by a clean
provider exit (`exit 0`) at `09:37:11Z`. The mounted terminal visibly rendered
the Codex TUI before Relay Runner changed to `Session exited (0)`.

Current readiness logic schedules `running` after any provider output observed
after the `provider_spawn` marker. That proves bytes reached SwiftTerm but not
that the PTY/provider lifetime is stable. Determine why the live provider
receives EOF, loses its PTY, or otherwise exits, and correct the ownership
boundary rather than merely delaying or suppressing the exit state.

## Acceptance criteria

- [x] A deterministic regression test reproduces a provider that renders interactive output and then exits cleanly immediately after apparent readiness.
- [x] Start Session keeps the embedded provider and its controlling PTY alive until an explicit provider, user, or app-owned termination.
- [x] Interactive readiness is based on a stable provider condition; arbitrary post-spawn output alone cannot turn a startup failure into a successful running session.
- [x] A clean exit during the startup/readiness grace period is shown as an actionable launch failure with the exact lifecycle stage, rather than the ambiguous `Session exited (0)` state.
- [x] Bridge cleanup occurs for failed starts without stopping a healthy bridge for a genuinely running session.
- [x] Lifecycle diagnostics remain owner-only, privacy-safe, and emit an unambiguous ordered launch, readiness, and termination sequence.
- [x] Codex and Claude launch paths have equivalent lifetime, diagnostics, retry, and cleanup coverage; intentional CLI-specific differences are documented.
- [x] Focused Swift tests cover real `LocalProcess`/PTY behavior or the nearest deterministic integration boundary, not only an in-memory phase model.

## Post-merge smoke

- [ ] After the reviewed branch is merged and one combined app is installed,
  physically start an embedded session and confirm it remains interactive and
  accepts input after the former one-second failure window. This is an explicit
  foreground/human gate because workers may not write the live
  `~/Library/Application Support/relay-runner/` session state.

## Evidence

- Installed app: `/Applications/Relay Runner.app`, version 0.4.27 build 31.
- Installed/package binary SHA-256:
  `a5f648f16dc889aedf0a10f035488dc6f52738f1979a2e1d6829584077e95090`.
- The bridge was healthy and waiting for voice input; no bridge error lines were recorded.

## Run log

- **Run 38** (attempt 7) — branch `relay/rr-258`
- The owner-only action journal placed a queued Codex command claim in the same second as provider spawn/readiness. App-owned delivery now waits until the provider owns a foreground, noncanonical PTY for a stable interval; equivalent real-PTY coverage exercises Codex and Claude.
- A 0.65-second clean-exit fixture failed before the fix after being observed as `running`, then passed after the fix as an `interactive_provider_readiness` failure. Focused Swift and Python tests pass; the full 600-test suite has the same nine unrelated model-catalog failures on current `main`.
- Source work is done with ordered privacy-safe schema-v4 diagnostics. Packaging/installing was intentionally not performed, and the post-merge human smoke remains unchecked.
