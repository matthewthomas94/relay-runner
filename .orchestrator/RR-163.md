---
id: RR-163
title: Restore integrated-terminal voice delivery after Codex startup
status: backlog
priority: urgent
depends_on: [RR-162]
canceled: false
worker_model: balanced
worker_effort: high
worker_sizing_rationale: "The fix is narrow, but it sits on the SwiftTerm PTY input boundary and must preserve both terminal-protocol replies and protection for partially typed user prompts."
worker_provider_notes: "Verify the origin distinction is terminal-emulator based rather than Codex-specific so Claude receives equivalent behavior."
---

## Description

Fix app-owned voice delivery in the integrated terminal. After RR-162, Codex and the bridge start successfully, but SwiftTerm terminal-protocol replies emitted during TUI startup flow through the same delegate as keyboard input. `RelayTerminalInputTracker` counts printable bytes inside those escape sequences as unsubmitted user text, so `/tmp/voice_cmd_ready` remains queued forever while the heartbeat misleadingly stays healthy.

Distinguish terminal-emulator response traffic from actual user-originated PTY input before updating the partial-prompt tracker. Preserve protection for genuinely typed or pasted prompts, normal return/backspace/clear behavior, app-owned command injection, and Codex/Claude parity.

## Acceptance criteria

- [x] Terminal-protocol replies generated during Codex or Claude TUI startup do not mark the prompt as partially typed.
- [x] Real typed and pasted prompt text still defers voice-command delivery until it is submitted or cleared.
- [ ] A voice turn in a live integrated session is claimed and submitted to the foreground provider.
- [ ] The app heartbeat and delivery claim accurately reflect a usable foreground consumer.
- [ ] Focused regression tests, the full Swift suite, Python tests, and a signed installed-app smoke test pass.

## Run log

- Reproduced against the signed installed app: the bridge accepted command `67453-1784116963019270000-1`, but `/tmp/voice_cmd_ready` remained unclaimed while the app heartbeat, Codex process, launchd bridge, and messenger all stayed alive.
- Implemented explicit SwiftTerm input-origin tracking: terminal-emulator replies cross the PTY delegate without entering the partial-prompt guard, while actual keyboard and paste sends still do.
- Verified 5 focused regression tests, 336 Swift tests, 208 Python tests, shell/Python syntax, generated instructions, and `git diff --check`. Rebuilt, Developer-ID signed, reinstalled, and relaunched the matching `/Applications/Relay Runner.app` bundle.
- Final integrated Start Session click remains pending because the reinstalled app does not currently have macOS Accessibility or Screen Recording permission; RR-163 stays open until the ready-to-claimed live smoke is observed.
- A later live session proved the daemon, socket, heartbeat, and messenger were healthy, but typing `/rel` left delivery blocked because Codex encodes Control-U and other edits with the negotiated Kitty keyboard protocol. The tracker now decodes Kitty press/repeat/release, modifier, submit, clear, and backspace events. App-owned startup also prints an explicit `Voice bridge ready.` line so successful startup is no longer visually indistinguishable from a hang.
- Reverified 6 focused tests, 337 Swift tests, 208 Python tests, shell/Python syntax, generated instructions, signed bundle integrity, and installed-bundle equality. A fresh manual integrated-session delivery smoke is still required before closing this ticket.
