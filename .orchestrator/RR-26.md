---
id: RR-26
title: Verify Codex worker activity chips are semantic
status: ready
priority: medium
depends_on: []
run_id: null
canceled: false
---

## Description

RR-16 added semantic activity chips so Codex workers should show human-facing progress such as "Running Swift build" or "Reading source files" instead of raw commands like `/bin/zsh -lc ...`. During the current session, the live run index still showed raw Codex command strings for active workers.

This may simply be a stale orchestrator daemon that predates the RR-16 code. Verify the behavior with a fresh daemon/code path. If raw commands still appear, fix the Codex activity ingestion/classification path.

Likely touchpoints:

- `services/orchestrator.py` (`derive_codex_activity`, `_semantic_shell_activity`, worker event handling)
- Board run-state display only if the daemon is publishing semantic text but the UI renders something else.

## Acceptance criteria

- [ ] Restart or otherwise ensure the running orchestrator daemon is using current code before testing.
- [ ] Dispatch or observe a Codex worker performing common actions: file reads, source edits, `swift build`, git status/diff/log, and ticket run-log updates.
- [ ] The board/run index shows semantic summaries, not raw `/bin/zsh -lc ...` command strings, for those common actions.
- [ ] If a raw-command case remains, add focused classifier coverage for that event shape.
- [ ] Raw command detail remains available in `.relay/run.log`; only the board-facing activity summary is cleaned up.
- [ ] Python syntax/tests or focused verification pass.
