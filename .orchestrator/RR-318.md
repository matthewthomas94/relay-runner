---
id: RR-318
title: Match Workspace setup loading style to update check
status: done
priority: high
execution_mode: implementation
depends_on: []
run_id: 1
canceled: false
worker_model: fast
worker_effort: low
worker_sizing_rationale: "A localized SwiftUI presentation and regression-test change with a known reference style and narrow blast radius."
worker_provider_notes: "none"
---

## Description

The initial Workspace loading state currently renders “Setting up your project...” with the smaller, muted empty-state treatment. Style that loading message the same way as the preceding “Checking for updates” screen so the startup sequence has consistent centered typography and contrast. Preserve the existing copy, timing, loading behavior, background, and accessibility behavior.

## Acceptance criteria

- [ ] The “Setting up your project...” loading message uses the same typography, foreground emphasis, and centered placement as “Checking for updates.”
- [ ] The loading-state copy, state transitions, timing, and hit-testing behavior remain unchanged.
- [ ] Reduced Motion and other accessibility settings continue to behave as before; this change adds no new motion.
- [ ] A focused regression test protects the shared visual presentation contract.

## Provider parity

This is app UI behavior and must be identical for Codex and Claude sessions.

## Run log

### Run 1 (attempt 1) — branch `relay/rr-318`

- Updated the Workspace setup loading presentation to use the same section-heading typography and white 0.92 foreground emphasis as “Checking for updates,” preserving copy, full-surface centering, hit testing, timing, and motion behavior.
- Added a focused `ProgramBoardStatusTests` regression covering the shared typography and foreground contract.
- `swift test --filter ProgramBoardStatusTests` passed: 71 tests, 0 failures. The initial `xcodebuild` command was not applicable because this checkout uses `Package.swift` and has no `.xcodeproj`.

Ticket done; no unresolved follow-ups.
