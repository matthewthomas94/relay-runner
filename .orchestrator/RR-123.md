---
id: RR-123
title: Prototype PM-frontstage orchestration flow
status: ready
priority: high
depends_on: []
run_id: null
canceled: false
order: 123
worker_model: strong
worker_effort: xhigh
worker_sizing_rationale: Cross-cutting voice/orchestrator prototype with architecture, event-contract, and provider-parity implications.
worker_provider_notes: Codex and Claude both support xhigh effort; keep PM/orchestrator behavior provider-neutral and document any intentional provider-specific differences.
---

## Description

Prototype the "Project Manager frontstage, orchestrator backstage" architecture discussed in the Relay voice session.

The product goal is perceived responsiveness: the user should hear immediate, useful acknowledgement and live progress while a backstage orchestrator is still thinking, planning, creating tickets, or deciding whether to execute solo or request workers.

For this prototype, avoid a full rewrite. Build the smallest slice that proves the shape:

- The PM/frontstage layer is the user-facing authority for acknowledgement, narration, cancellation/preemption, and dispatch decisions.
- The orchestrator/backstage layer owns technical planning and returns structured outcomes such as `execute_solo`, `delegate_plan`, or `needs_user`.
- Any worker fan-out is requested through a structured delegation plan rather than spawned ad hoc by the orchestrator.
- Public status events are structured and user-safe; do not stream raw hidden reasoning.
- The flow preserves Relay command metadata so stale voice commands cannot create tickets, dispatch workers, or speak outdated TTS.

Prefer reusing existing voice bridge, `voice_state.sock`, TTS FIFO, and orchestrator run/dispatch infrastructure. Keep the prototype behind an experimental path or narrow integration point if enabling it broadly would be risky.

## Acceptance criteria

- [ ] Define a small PM/orchestrator status-event contract in code or docs, including phase, user-facing message, source, command metadata, and optional run/ticket references.
- [ ] Prototype a PM-frontstage path that can immediately acknowledge a voice command before backstage planning completes.
- [ ] Prototype a backstage orchestrator outcome shape for at least `execute_solo`, `delegate_plan`, and `needs_user`.
- [ ] Ensure any proposed worker dispatch remains PM-controlled and carries Relay command metadata for stale-action protection.
- [ ] Include Codex and Claude provider-parity notes for how the flow behaves under each worker provider.
- [ ] Add focused tests or a deterministic harness proving the status-event contract and stale-command handling for the prototype path.
- [ ] Document how to manually exercise the prototype and what latency checkpoints should be measured next.
